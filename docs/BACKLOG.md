# ReCodEx New Frontend — Backlog

**Status:** Draft
**Date:** 2026-05-11

---

## Legend

| Status    | Meaning                                        |
| --------- | ---------------------------------------------- |
| `todo`    | Ready to work                                  |
| `doing`   | Currently in progress                          |
| `done`    | Completed and verified                         |
| `blocked` | Cannot proceed — reason recorded               |
| `dropped` | Intentionally omitted — reason in `DROPPED.md` |

---

## Foundation (Phase 1)

| ID    | Title                                                       | Inventory Rows   | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ----------------------------------------------------------- | ---------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | Scaffold Next.js 16.3 repo with App Router, TS strict, pnpm | —                | done   | 16.3.1 confirmed latest via `npm view`. `typecheck`/`lint`/`build` all green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-002 | ESLint flat config + Prettier + TypeScript 7                | —                | done   | **TypeScript pinned to 6.0.3, not 7** — `typescript-eslint`/`eslint-plugin-react` don't support TS7/ESLint10 yet, see AGENTS.md "Toolchain deviations". `pnpm typecheck`/`lint` both work standalone; no pre-commit hook wired yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-003 | Dockerfile with `output: 'standalone'`                      | —                | done   | Lives in this repo's own root (`Dockerfile`), not `services/<name>/` — this repo isn't one of the pull-repos.sh-managed upstream ReCodEx repos, so it doesn't share their build-context pattern; see DECISIONS.md. `docker build` + `docker run` both verified working end to end, real HTTP 200 with rendered HTML.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-004 | Compose service entry (self-contained add)                  | —                | done   | Diff shown, operator approved as-is. `web-next` service added to `../ReCOdex/docker-compose.yaml` (DEC-028); `WEB_NEXT_PORT` added to `../ReCOdex/.env`/`.env.example`. Verified live: `docker compose up -d web-next`, HTTP 200, and `api` reachable internally via `getent hosts api`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-005 | CI pipeline (GitHub Actions)                                | —                | done   | `.github/workflows/ci.yml`: `typecheck`, `lint`, `build`, `test`, via `corepack enable` (matches Dockerfile, not `actions/setup-pnpm`). Added `vitest.config.ts` (`passWithNoTests: true` — no tests exist yet, F-023/F-024 land the harness) and `"type": "module"` to `package.json` (silenced a real Vite config-loader warning, not just cosmetic). All four steps verified locally, including `build` with `.env.local` removed to simulate CI's actual environment. **Gap found and fixed during F-010:** `format:check` existed as a script since F-002 but was never added to this pipeline, and the whole repo had never actually been run through Prettier — `pnpm format:check` failed on 15 files, none touched by F-010 itself. Fixed with `pnpm format` (repo-wide reformat, whitespace/table-alignment only, no logic changes) and added `format:check` as a fifth CI step.                                                                                        |
| F-006 | Runtime config: `.env.local` + `.env.example`               | `config.js`      | done   | `API_BASE_INTERNAL`/`API_BASE_PUBLIC`/`MONITOR_WS_URL` set to verified real values (docs/QUESTIONS.md Q-001/Q-006), not placeholders. CAS/SMTP vars deferred to their own auth tickets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F-007 | `next.config.ts`: `basePath` from build arg                 | `config.js`      | done   | Already landed as part of F-003: `Dockerfile`'s `ARG URL_PATH_PREFIX` + `next.config.ts`'s `basePath`. Build-time limitation documented in `DROPPED.md`. Noticed and closed out while updating this table for F-025, not separately implemented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F-008 | Turbopack filesystem cache for builds                       | —                | done   | `turbopackFileSystemCacheForBuild` defaults to `true` (confirmed in `node_modules/next/dist/server/config-shared.d.ts`'s default-config export) — no `next.config.ts` change needed. Verified for real, not just from the default: `.next/cache/turbopack/v16.3.1-*/` is populated with real cache files (SST/LOG/CURRENT) after a build, and a warm second build compiles in 254ms vs. 1673ms cold — ~6.5x.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-009 | Turbopack memory eviction in dev                            | —                | done   | `turbopackMemoryEviction` defaults to `'auto'` (`config-shared.d.ts`) -- no config change needed. Verified with a real (if short) `next dev` session: RSS dropped from ~457MB to ~351MB (23%) over 60s idle with zero requests in between, consistent with active eviction. A fuller "does memory stay bounded over hours of real use" test isn't meaningful yet -- this repo only has 2 placeholder routes, nothing substantial to evict. Revisit once real route/module volume exists (Design System phase onward).                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-010 | Theme tokens (dark/light)                                   | `config.js` SKIN | done   | Tailwind CSS v4 (not previously installed — brought in as part of this ticket) + shadcn/ui-compatible CSS custom properties in `app/globals.css` (`:root`/`.dark`, `oklch`, mapped via `@theme inline`) + `next-themes` for the class toggle. See DEC-032. Verified via compiled build output (`.next/static/chunks/*.css` has both blocks with genuinely different values; `body`/`*` rules correctly resolve through the custom properties) and the rendered HTML of a real `next start` (next-themes' anti-FOUC script present and correctly configured). No component-level browser click-through yet — nothing to click on besides the placeholder page; full visual verification belongs with the first real UI ticket (D-series).                                                                                                                                                                                                                                          |
| F-011 | i18n: next-intl with cs/en                                  | `locales/`       | done   | `next-intl@4.13.7`, `app/[locale]/...`, `localePrefix: "always"`. Restructured `app/layout.tsx`/`app/page.tsx` into `app/[locale]/layout.tsx`/`page.tsx` (the `[locale]` layout _is_ the root layout now). `proxy.ts` (not `middleware.ts`) wraps `next-intl/middleware`. `messages/{en,cs}.json` started minimal (just the placeholder page) — full legacy message migration happens per-screen as S-/T-/D-series tickets land, not as a bulk port. See DEC-033. Verified via `next build`, a live `next dev` session, and a real `docker build`+`docker run` (proxy/middleware bundling under `output: standalone` has a history of subtle breakage, so didn't trust `next dev` alone).                                                                                                                                                                                                                                                                                         |
| F-012 | Error/not-found/forbidden conventions                       | `NotFound` page  | done   | `app/[locale]/{error,not-found,forbidden,unauthorized}.tsx` + `app/{global-error,global-not-found}.tsx`. `forbidden()`/`unauthorized()` need `experimental.authInterrupts`; the bogus-URL-under-valid-locale case needed `experimental.globalNotFound` too (found live, not from docs — see DEC-034). `catchError` turned out unnecessary for route-segment boundaries; standard `error.tsx`'s `retry` prop (stable since 16.3.0) already covers it. Verified via temporary test routes hitting all four states (403/401/500/404) for real HTTP status + rendered content, in `next dev` and a full `docker build`+`run`, then deleted before committing.                                                                                                                                                                                                                                                                                                                         |
| F-013 | Route skeleton with layouts                                 | All pages        | done   | `(anon)`/`(app)` route groups under `app/[locale]/...`. All 16 top-level static routes from `docs/IA.md`'s sitemap stubbed with a shared `PlaceholderPage`, titles sourced from the legacy app's real translations, not guessed. Dynamic sub-routes (`/groups/[groupId]`, etc.) deliberately deferred to their owning S-/T-/A-/AD- ticket. See DEC-035. Verified via `next build`'s route list, a live `next dev` session, and a full `docker build`+`run`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F-014 | `proxy.ts` for auth UX redirect                             | `auth` module    | done   | Presence-only session-cookie check (never validates — that's F-015's job). Composed with next-intl's own middleware from F-011 by short-circuiting on its redirects. Three-way pathname split (auth-only, public, everything-else) kept in sync by hand with F-013's route groups. `lib/auth/session-cookie.ts` holds the shared cookie-name constant F-016 will reuse. See DEC-036. Verified all six presence/pathname combinations plus the chained locale→auth redirect with real requests, in `next dev` and a full `docker build`+`run`.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-015 | `requireSession()` server-side guard                        | `auth` module    | done   | `lib/auth/require-session.ts`. Decodes the session cookie's JWT payload (no library, no signature verification -- core-api is the real authority), checks `exp`, redirects to `/login` on failure. Not wired into `(app)/layout.tsx` -- brief §5's explicit "not the layout," each data-touching function guards itself once F-022 lands. See DEC-037. Verified live via a temporary test route (deleted before commit): no cookie, a real valid JWT from an actual login, an expired token, and a malformed cookie -- all four behave correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F-016 | Auth BFF: login Route Handler                               | `auth` module    | done   | `app/api/auth/login/route.ts`. `{email, password}` -> core-api `username`/`password`; `sessionCookieOptions()` (`lib/auth/session-cookie.ts`) sets httpOnly/sameSite=lax/`secure` derived from `API_BASE_PUBLIC`'s scheme, `maxAge` from the JWT's real `exp`. Corrects DEC-023 (login is a Route Handler, not a Server Action -- brief §5's own words). See DEC-038. Verified for real: correct/wrong credentials, malformed input, and the full login-cookie-`proxy.ts`-`requireSession()` round trip, in both `next dev` and the production compose container over the internal Docker network.                                                                                                                                                                                                                                                                                                                                                                                |
| F-017 | Auth BFF: logout Route Handler                              | `auth` module    | done   | `app/api/auth/logout/route.ts`. POST only, clears the session cookie, 303s to `/login`. core-api has no logout endpoint (confirmed against swagger) -- BFF-side only. Found and fixed a real bug along the way: `request.url` is unreliable under `output: standalone` (reflects the container's internal bind address, not the actual `Host`) -- see DEC-039. Verified the full login-then-logout round trip in both `next dev` and the production compose container.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F-018 | Auth BFF: token refresh in `proxy.ts`                       | `auth` module    | done   | `lib/auth/refresh-session.ts`'s `maybeRefreshSession()`. Proactive refresh 24h before `exp`, in-memory per-token de-duplication (module-scope `Map`). Found live: core-api doesn't invalidate the old token on refresh (changes the failure mode from "invalidate each other" to "redundant call"), and its JWTs are second-granularity deterministic (a real gotcha for testing de-duplication by comparing token values). See DEC-040. Verified by counting actual upstream calls: 6 concurrent requests sharing one soon-to-expire cookie produced exactly 1 refresh call.                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-019 | Auth BFF: CAS callback Route Handler                        | `auth` module    | done   | `app/api/auth/external/[authenticatorName]/callback/route.ts`. Generic dynamic segment, not hardcoded to CAS -- reads a `token` query param, POSTs to core-api's `/login/{authenticatorName}`, reuses F-016's `establishSession()`. Mechanism resolved by reading core-api's own PHP source, not guessed; see DEC-041. Verified live (both `next dev` and Docker `standalone`) that `request.url`'s query string is safe to read despite DEC-039's host-portion bug. Success path against a real external provider is unverifiable here -- no `EXTERNAL_AUTH_*` configured (`QUESTIONS.md` Q-004).                                                                                                                                                                                                                                                                                                                                                                                |
| F-020 | Auth BFF: user takeover                                     | `auth` module    | done   | `app/api/auth/takeover/[userId]/route.ts`, mirrors core-api's `/login/takeover/{userId}` 1:1. Authorization fully delegated to core-api's `canTakeOver()`/`permissions.neon` (superadmin-only, confirmed by reading the neon config) -- this route only checks a session exists, forwards it, and surfaces core-api's 403/404 as JSON. Verified live (dev + Docker) against real seeded accounts: superadmin takeover succeeds and swaps the session's `sub`; non-superadmin gets 403; no session gets 401; malformed `userId` gets 400. See DEC-042.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-021 | Auth BFF: restricted token generation                       | `auth` module    | done   | `app/api/auth/restricted-token/route.ts`. Self-service "Generate Application Token" primitive (legacy `GenerateTokenForm.js`) -- deliberately returns the raw token in the JSON response (the one intentional exception to DEC-021) and never touches the session cookie. Scoped narrowly to this primitive, not the separate `restrictEffectiveRole()` session-swap UX also found in the legacy app; see DEC-043. Verified live (dev + Docker): scoped token decodes to exactly the requested scopes, caller's session cookie unchanged; core-api's own validation errors (forbidden scope, unknown role, expiration cap) forwarded verbatim.                                                                                                                                                                                                                                                                                                                                    |
| F-022 | Typed `server-only` API client                              | `apiMiddleware`  | done   | `lib/api/client.ts` + generated `lib/api/core-api.generated.ts` (`pnpm generate:api-types`, vendored spec `openapi/core-api.yaml`). `path` typed against `keyof paths` -- real compile-time endpoint checking -- but response types stay hand-written: core-api's swagger has zero response schemas (confirmed: ~250 "Placeholder response" entries, no `components.schemas`), so `openapi-fetch`'s typed-response value doesn't apply here. `requireSession()` on every call, `cache: "no-store"` always, `ApiError` normalizes core-api's `{success, error, payload}` envelope using the same short `code` strings (`"403-002"`) the legacy app's own `apiErrorMessages.js` keys on. Found and fixed a real double-`/v1`-prefix URL bug live (both `API_BASE_INTERNAL` and every generated path key include `/v1`). See DEC-044. Verified live (dev + Docker) via a temporary debug route: real `GET /v1/users/{id}` success and a genuine core-api 404 both surface correctly. |
| F-023 | Playwright harness skeleton                                 | —                | done   | `playwright.config.ts` + `e2e/`. Logs in via the real Auth BFF login route (no form exists yet), runs against `next build`+`next start` (not dev), Chromium only, `workers: 2` (found live: default worker count overwhelms the local dev core-api's PHP-FPM pool with concurrent bcrypt logins). Skipped `@next/playwright` -- its `instant()` helper requires Cache Components, which this repo deliberately has off (DEF-001). Not wired into CI (no reachable core-api there). See DEC-045 for the 3 real bugs found while building this (private-folder routing, `test.use({storageState})` describe-scope leakage, worker concurrency). 48/48 tests pass stably: 8 public routes + 4 seeded personas x 10 protected routes each.                                                                                                                                                                                                                                            |
| F-024 | Token-leakage security test                                 | —                | done   | `e2e/security.spec.ts`, brief §8's "non-negotiable" test. Uses real `page.request.post()` login (not F-023's `context.addCookies()` shortcut, which would make the httpOnly check tautological) so Playwright parses the actual `Set-Cookie` header; finds the session cookie by diffing `context.cookies()` rather than assuming its name. Scans every protected route's full HTML plus every served JS bundle for the raw token. Deliberately verified both assertions can actually fail (temporarily flipped each, confirmed a real failure, reverted) before trusting either. See DEC-046.                                                                                                                                                                                                                                                                                                                                                                                    |
| F-025 | `scripts/seed.ts` — test data generator                     | —                | done   | API-only, idempotent (verified: a clean re-run against fully-seeded state produces zero creates), `[seed]` prefix. Builds 4 groups, 3 named users + 25 pagination-filler students, 1 fully-configured gradeable exercise reused across 27 assignments, and 2 real submissions. See DEC-029/030/031 for the exercise-build recipe and the cgroup v1 evaluation caveat.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-026 | `docs/SEED_ACCOUNTS.md`                                     | —                | done   | Rewritten to match what the script actually builds (the recon-phase plan used different group/account names).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## Design System (Phase 2)

| ID    | Title                                               | Inventory Rows      | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | --------------------------------------------------- | ------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-001 | `PageShell` component                               | All pages           | done   | `components/page-shell.tsx`, per `docs/IA.md` §3.3. Plain Server Component -- `breadcrumbs` is required but unresolved (D-002's job); `actions`/`tabs` are pre-built `ReactNode` slots. Wired into `components/placeholder-page.tsx` (all 18 current routes) with a single unlinked crumb as an immediate live-verification path. See DEC-047. Verified live (dev + Docker): full 50-test E2E suite passes unchanged; a temporary debug route confirmed `subtitle`/`actions`/`tabs`/linked-crumb all render correctly, including a correctly locale-prefixed `href`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-002 | Central breadcrumb manifest                         | All pages           | done   | `lib/breadcrumbs/manifest.ts`. Flat `{namespace, pattern}`/`{pattern, resolve}` registry, prefix-walked per pathname, throws on any unregistered route (confirmed live: clean 500, not a silent gap). Nested dynamic segments (`/groups/[groupId]` etc.) deliberately unregistered -- no page exists yet to test a resolver against; type signature ready for whichever ticket builds that route. Wired into `components/placeholder-page.tsx`, simplifying all 17 stub `page.tsx` files (namespace-only prop, no longer `async`). See DEC-048. Verified live (dev + Docker): two-segment `/forgot-password/change` renders a correct two-crumb chain with a properly locale-prefixed link; full 50-test E2E suite passes unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-003 | `DataTable` component                               | All list views      | done   | `components/data-table.tsx`. Client-side sort/filter/paginate over a caller-supplied dataset (legacy `SortableTable`'s own model, read first), but URL-synced (`${id}-sort/-q/-page`) instead of legacy's `localStorage` -- a deliberate correction. Selection stays local state, not URL-synced. Two real bugs found live: `columns` (function props) can't cross the Server-> Client boundary as a prop (documented prominently); a Docker prerender-time failure (`useSearchParams()` needs `Suspense`) that `next dev` never caught -- fixed by wrapping internally so every future caller is unaffected. See DEC-049. Verified live (dev + Docker, specifically re-verified against Docker after the Suspense fix): sort/paginate/filter compose correctly together; selection never touches the URL. **Note (DEC-050)**: brief §4 names TanStack Table as the intended library; investigated its actual v9 API afterward and found a substantial headless/feature-registration rewrite that wouldn't reduce this component's URL-sync work at all -- documented as a verified, deliberate non-adoption, not an oversight.                                                                                                                                    |
| D-004 | Form kit on Server Actions                          | All forms           | done   | `lib/forms/{action-result,use-server-action-form,use-dirty-guard}.ts`, `components/form/{text-field,form-error}.tsx`. RHF's `onSubmit` calls the Server Action directly (not `<form action>` -- the two submission models would conflict), wrapped in `startTransition`. Server errors map onto RHF's own `setError`/`root` error model. `useDirtyGuard` covers `beforeunload` only -- checked bundled docs, confirmed no in-app navigation-blocking primitive exists in the App Router as of 16.3. Real bug found live: a Zod schema co-located in the "use server" file broke at runtime (only function exports survive that compiler pass) -- fixed by splitting the schema into its own shared module. See DEC-051. Verified live (dev + Docker) via a real demo Server Action: client validation, server field error, server form-level error, success, and the dirty-guard state all confirmed working.                                                                                                                                                                                                                                                                                                                                                      |
| D-005 | Upload component (Route Handler)                    | `upload` module     | done   | `lib/upload/{limits,chunked-upload,use-file-upload}.ts`, `components/upload/file-upload.tsx`, `app/api/upload/partial/route.ts`, `app/api/upload/partial/[id]/route.ts`, `app/api/upload/[id]/digest/route.ts`. Brief §6.7's open question answered: the legacy app **does** chunk, via core-api's per-partes protocol (start / `PUT ?offset=N` per chunk / complete / SHA-1 digest check / cancel) with adaptive chunk sizing (64 KiB start, ×2 under 2 s, ÷2 over 4 s, clamped 4 KiB–4 MiB) -- reproduced exactly. Chunk loop stays in the browser so progress, cancel and the checksum check are real; each chunk is proxied through a Route Handler that attaches the session token server-side and forwards the body as an unbuffered stream. 512 MiB ceiling verified against the deployment's own nginx/PHP config, enforced in three layers. Verified live against the running stack (bare host _and_ the Docker `standalone` image): a 300 KiB file uploaded in 5 chunks with core-api's SHA-1 matching the local file byte for byte, plus 401/oversize/bad-filename/bad-uuid/offset-mismatch/cancel/complete-after-cancel all rejecting correctly, and a browser run of the real component showing progress, cancel and the uploaded state. See DEC-053. |
| D-006 | Dialog/modal system                                 | Various             | done   | `components/dialog/{dialog,confirm-dialog}.tsx`, `Dialog` strings in both locales, dialog motion tokens in `app/globals.css`. On the unified `radix-ui` package (1.6.7) rather than per-primitive packages -- more primitives are coming (D-007, D-015, dropdowns/tooltips) and one version that moves together beats a dozen drifting ranges. `ConfirmDialog` is built on `AlertDialog`, not `Dialog`: `role="alertdialog"`, initial focus on Cancel, and no outside-click dismissal, all verified in the installed source. Two stale-by-a-version details checked rather than copied from memory: 1.1.23 omits `aria-describedby` on its own when no description is rendered (the `aria-describedby={undefined}` workaround is obsolete), and it ships no console warnings at all, so a missing title now fails silently -- hence `title` being a required prop. Motion respects `prefers-reduced-motion`. Verified in a real browser: focus trap, Escape, focus restored to the trigger, accessible name/description, backdrop click ignored by the confirm dialog, Cancel focused first. One real bug found by looking at a screenshot -- see DEC-054.                                                                                                         |
| D-007 | Toast notification system                           | Various             | done   | `components/toast/toast-provider.tsx`, mounted once in `app/[locale]/layout.tsx`, reached via `useToast()`. On Radix `Toast` (same family as D-006, DEC-054) for its `role="status"`/`aria-live` handling, hover- and blur-paused timers, swipe dismissal and the F8 focus hotkey. Errors are `foreground` (assertive, 8 s), successes `background` (polite, 4 s). `useToast()` throws outside the provider rather than no-op'ing -- a toast that silently does nothing is the exact failure brief §9 forbids.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-008 | State components (loading, empty, error, forbidden) | All lists           | done   | `components/state/{status-state,skeleton,empty-state,error-state,error-boundary}.tsx`. One `StatusState` layout behind not-found/forbidden/unauthorized/empty/error so they cannot drift apart; those four route pages rewritten onto it. `loading.tsx` added at each route-group level (covers every nested segment). `ErrorBoundary` wraps `catchError` from `next/error` -- verified present in the installed next@16.3.1 -- for panel-level failures, with Next's own `retry()` rather than a bespoke refetch. Skeletons hold still under `prefers-reduced-motion`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-009 | Code viewer with line anchoring                     | `solutions/[id]`    | done   | `components/code/code-viewer.tsx`, `lib/code/{highlight,languages}.ts`. Shiki 4.4.3, server-side only -- no highlighter, grammar or theme JSON reaches the browser, and the code is in the initial HTML (readable pre-hydration, findable with the browser's own Ctrl+F). Extension→language map ported from the legacy `syntaxHighlighting.js` so files look the same as students are used to. Every line gets a stable `id` (`L12`) and a real anchor; `:target` highlights the linked line with no JS. Dual theme via `defaultColor: false` + CSS keyed on next-themes' `.dark`. Files over 512 KiB render unhighlighted and say so. Per-line review comments and collapsed regions stay with the solution-review ticket -- the `data-line` hook they need is here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-010 | Markdown renderer                                   | `exercises/[id]`    | done   | `components/markdown/markdown.tsx`, `lib/markdown/legacy-compat.ts` + unit tests. Brief §7's required comparison was run: 16 constructs through both renderers, 10 differed. The two that would damage authored content -- raw HTML being dropped entirely, and `$5 and $10` turning into mathematics -- are fixed by compatibility plugins pinned with unit tests; the rest (GFM autolinks, task lists, footnotes) are additive and recorded. Fences use D-009's Shiki instance. See DEC-055.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-011 | Status badges                                       | All status displays | done   | `components/status/{badge,evaluation-badge,deadline-badge}.tsx`, `lib/status/evaluation.ts` + unit tests, `--success`/`--warning` theme tokens. Evaluation logic ported from the legacy `SolutionStatusIcon` decision tree and unit-tested (three of its six outcomes are infrastructure failure modes that cannot be reproduced on this cgroup-v2 machine at all). Deadline state renders client-side only via `useSyncExternalStore` -- it depends on the current time, which is the hydration trap AGENTS.md §6.6 warns about. Permission badges deliberately not built: they need real ACL fields from a screen that does not exist yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-012 | Formatters (date, points, relative time)            | All pages           | done   | `lib/format/points.ts` + unit tests, `components/format/{date-time,relative-time}.tsx`, and an explicit `timeZone` in `i18n/request.ts`. The time zone was the real finding: without it a Server Component formats in the container's zone while the browser uses the user's, which is both a hydration mismatch and a wrong deadline. Pinned to the deployment's zone (`APP_TIME_ZONE`, default `Europe/Prague`) -- also more correct for the product, since a deadline announced as 23:59 means that wall-clock time to everyone discussing it. Relative times render client-side only. `formatPercent` floors rather than rounds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-013 | `/dev/kitchen-sink` route                           | —                   | done   | `app/[locale]/dev/design-system/page.tsx` (Server Component) + `components/dev/design-system.tsx` (the one `"use client"` island). Renders theme tokens, `DataTable`, the form kit's error states, both dialog kinds and the upload component. Public in `proxy.ts` -- it shows no user data, and requiring a session would only make it harder to look at; the upload section does hit core-api and says so. Taken ahead of D-007 at the operator's request for a visible page to watch progress on. Route renamed from this ticket's original `/dev/kitchen-sink` at the operator's request -- see PROGRESS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-027 | Dependency review process                           | —                   | done   | Originally `.github/dependabot.yml`; **reverted at the operator's request** after it opened three PRs against `main` on first run. Replaced by `pnpm deps:check` (`pnpm outdated` + `pnpm audit`), run on request rather than on a schedule. Brief §4's intent -- not missing a Next.js security release -- is met by the check plus GitHub's passive Dependabot _alerts_, which need no config and open no PRs. See DEC-056.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-016 | Design-system regression spec                       | —                   | done   | `e2e/design-system.spec.ts`. D-003..D-009 each built a throwaway demo page, verified by hand and deleted it, leaving nothing that would catch a regression. Now that D-013's showcase is permanent and public, one spec covers dialogs, the confirm dialog's no-outside-dismiss guarantee, toasts, the error boundary's containment and recovery, and server-side highlighting with working line anchors -- with no login, so it costs none of the bcrypt round trips `playwright.config.ts` had to cut workers for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-028 | Re-evaluate the TypeScript 7 / ESLint 10 pin        | —                   | todo   | F-002 pinned TypeScript to 6.0.3 and ESLint to 9 because `typescript-eslint`/`eslint-plugin-react` did not support TS7/ESLint10 at the time -- a deliberate deviation from brief §4's stack table, which asks for TypeScript 7 specifically so `next build` uses the native type checker. The first `pnpm deps:check` run (2026-08-21) shows **typescript 7.0.2 and eslint 10.9.0 are both released now**, so the blocking condition may be gone. Verify the plugins actually support them before bumping; the whole point of the original pin was that they did not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-029 | Seed fixtures for unreachable UI states             | —                   | done   | `seed.newcomer` (no memberships), `[seed] Faculty of Seeded Studies` (organizational), and a third Intro to Programming assignment with a **second deadline** (10 points before, 5 after). Also fixed a real idempotency leak found on the way: the exercise's reference solution was submitted on every run, and eight had piled up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-014 | Sidebar / app-shell navigation                      | `(app)/layout.tsx`  | done   | `components/app-shell/{app-shell,sidebar-nav}.tsx`, `lib/api/{current-user,groups}.ts`, `lib/i18n-text/localized.ts`, `e2e/app-shell.spec.ts`. Sections per `docs/IA.md` §3.1, with visibility derived from **per-group membership** (`/v1/users/{id}/groups` returns the student/supervisor split already) rather than the global role, exactly as the IA insists. Server Component fetches; the client island only handles collapse, the phone drawer and active state. `aria-current="page"` rather than colour alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-015 | Command palette (Cmd/Ctrl-K)                        | `search` module     | done   | `components/command-palette/command-palette.tsx`, `app/api/search/route.ts`, `e2e/command-palette.spec.ts`. On `cmdk` for real combobox semantics, same reasoning as DEC-054's Radix choice. **No `/search` endpoint exists in core-api** -- the Route Handler queries the three list endpoints that genuinely accept `search` and normalises their two different response shapes (`/groups` returns an array, `/users` and `/exercises` a paginated envelope). Assignment search is not possible at all -- see Q-011. User search is attempted for everyone and allowed to 403, so permission comes from core-api rather than from a role check here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-030 | core-api 403/404 as a refusal, not an error         | —                   | done   | `lib/api/read.ts` (DEC-070): `apiRead`/`pageRead` turn a 403 into `forbidden()` and a 404 into `notFound()`, everything else still throws `ApiError`. Adopted by the whole DAL and by `lib/breadcrumbs/manifest.ts`; Route Handlers, Server Actions and the three reads that handle their own refusal (the dashboard's review queue, a group's ancestors, one file's content on the sources page) keep the raw client, since a `catch` around an interrupt swallows it. Verified live as a student against a real 403 and a real 404, plus `e2e/refusals.spec.ts`. The status code such a page answers with is still 200 -- that is Q-016, untouched here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-031 | An expired session reads as an error page           | —                   | done   | `proxy.ts`, `app/api/auth/session-expired/route.ts`, `isSessionTokenLive()` in `lib/auth/session-cookie.ts`, 401 handling in `lib/api/read.ts`. **The symptom was worse than this row said: an infinite redirect loop**, reproduced with `curl` (12 hops and still going) -- `proxy.ts` tested the cookie for _presence_, so a dead one counted as signed in and bounced `/login` back to `/dashboard`. Both layers now ask the same liveness question. core-api refusing a token whose `exp` has not passed (a password change's validity threshold) leaves through the new Route Handler, because a render cannot clear a cookie (DEC-089). A Server Action that hits the same 401 still shows core-api's own message in the form; it self-corrects on the reader's next navigation                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

---

## Student Experience (Phase 3)

| ID    | Title                              | Inventory Rows               | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ---------------------------------- | ---------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S-001 | Dashboard — student section        | `Dashboard` page             | done   | `app/[locale]/(app)/dashboard/page.tsx`, `components/dashboard/*`, `lib/api/dashboard.ts`, `lib/status/assignment-progress.ts` + unit tests, `e2e/dashboard.spec.ts`. Also fixed D-014's "My Teaching" (DEC-058) and added the `/assignments/[id]` route skeleton the deadline rows link to.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S-002 | Dashboard — teacher section        | `Dashboard` page             | done   | `components/dashboard/{teacher-section,review-queue}.tsx`, `getTeacherDashboard()` in `lib/api/dashboard.ts`, `/solutions/[id]` route skeleton, review fixtures in `scripts/seed.ts`. "Recent activity" is not built — no endpoint exists for it, see Q-013.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S-003 | Dashboard — calendar view          | —                            | done   | `components/dashboard/{calendar-section,deadline-calendar,section-nav}.tsx`, `lib/format/calendar-month.ts` + unit tests, `getDeadlineCalendar()`. Inventory row corrected: `userCalendars` is the legacy **iCal token manager**, not a calendar view — see Q-014, it belongs to S-022.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| S-004 | Group list                         | `groups` module              | done   | `app/[locale]/(app)/groups/page.tsx`, `components/groups/group-table.tsx`, `getGroupList()`. One table with a membership column rather than "mine" + "discover" — core-api already decides visibility. First real `DataTable` use; its chrome was hardcoded English until now. Client-side filtering, see Q-015.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| S-005 | Group detail — Info tab            | `GroupInfo` page             | done   | `app/[locale]/(app)/groups/[groupId]/page.tsx`, `components/groups/{group-info,group-tabs}.tsx`, `getGroupDetail()`. Tab in `searchParams` per IA §4.2; tabs appear only where `permissionHints` allows and the screen exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| S-006 | Group detail — Assignments tab     | `GroupAssignments` page      | done   | `components/groups/{assignment-table,assignment-filter}.tsx`, `getGroupAssignments()`. Filter is server-side and in the URL; the two personal columns and the "with my submissions" filter appear only for someone who studies in the group.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S-007 | Group detail — Students tab        | `GroupStudents` page         | done   | `components/groups/student-table.tsx`, `getGroupStudents()`. One row per student; the per-assignment points **matrix** is T-006's screen, where it can be sorted and exported at full width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| S-008 | Group detail — Exams tab           | `GroupExams` page            | done   | `?tab=exams` on the group page (DEC-071), `components/groups/exam-{status,form,table,roster,locks}.tsx`, `lib/api/group-exams.ts`, `lib/actions/group-exam.ts`, `lib/status/exam.ts`. Scheduling, editing, starting and ending an exam; the student's own lock; the live roster of who is locked in; and the lock records of a held exam. The seed now records one finished exam with a lock (DEC-073). The group's `exam` **flag** (`POST /v1/groups/{id}/exam`, "list this group among exam groups") is not here -- the legacy app puts it on the group's settings screen, so it belongs to S-009                                                                                                                                                                                                                |
| S-009 | Group detail — Settings tab        | `EditGroup` page             | done   | `?tab=settings` on the group page, `components/groups/{settings-form,settings-controls,member-manager,user-picker}.tsx`, `lib/actions/group-settings.ts`. The group's texts in every language, visibility, pass threshold or points limit, group type (organizational / exam flag -- the one S-008 left here), archiving **and unarchiving** (the action S-011 left here), relocation, deletion, and membership: students and staff, with roles. **Invitations are not here** (DEC-074): T-018 owns them and S-023 owns the page their links lead to. Fixed D-015's user search on the way (DEC-076)                                                                                                                                                                                                               |
| S-010 | Group hierarchy (nested subgroups) | `groups` module              | done   | Ancestry is a path column in the group list, the page subtitle, a parent link and a subgroup list on the Info tab — no tree widget, since the hierarchy is shallow and a breadcrumb plus a child list is keyboard- and screen-reader-navigable for free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| S-011 | Archived groups view               | `Archive` page               | done   | `app/[locale]/(app)/archive/page.tsx` — same table, `onlyArchived`. The unarchive action belongs to S-009 with the rest of group administration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| S-012 | Assignment detail — student view   | `Assignment` page            | done   | `app/[locale]/(app)/assignments/[assignmentId]/page.tsx`, `components/assignments/assignment-detail.tsx`, `lib/api/assignment.ts`. Reader-facing text is `text`, not `description` (students never have `viewDescription`). Submit state is stated in words; the action is S-014.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| S-013 | Assignment detail — teacher view   | `Assignment` page            | done   | `components/assignments/{class-progress,solver-table,exercise-sync-notice,solution-list}.tsx`, `lib/api/assignment-solvers.ts`, `app/[locale]/(app)/assignments/[assignmentId]/users/[userId]/page.tsx`. Class stats, the roster with each student's standing, teacher-only terms (visibility, release date, assigned at) and the exercise-synchronisation state. The **links** IA §4.3 also lists are T-002's and T-003's to add, since both screens are unbuilt (DEC-066).                                                                                                                                                                                                                                                                                                                                       |
| S-014 | Submit solution flow               | `assignment` routes          | done   | `app/[locale]/(app)/assignments/[assignmentId]/submit/page.tsx`, `components/assignments/submit-form.tsx`, `lib/actions/submit-solution.{ts,schema.ts}`. First real Server Action; files go through D-005's Route Handler, never the action. Found and fixed a seed bug: the exercise declared no source-file variable, so `pre-submit` could not detect a language.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| S-015 | Evaluation result display          | `Solution` page              | done   | `app/[locale]/(app)/solutions/[solutionId]/page.tsx`, `components/solutions/evaluation-results.tsx`, `lib/api/solution.ts`. One request carries the last submission's evaluation in full. **The test table has never been seen with data** — no solution on a cgroup v2 host evaluates (DEC-031).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| S-016 | Live evaluation progress           | `evaluationProgress` module  | done   | `components/solutions/evaluation-progress.tsx`. **Both** mechanisms, and DEF-004 closed with them (DEC-065): the page refreshes itself every 5s while the solution is pending (`router.refresh()`, so the result is rendered once, on the server, by the code that already renders it -- stopped when the tab is hidden and after 5 minutes), and the monitor WebSocket adds task-by-task progress when `?monitor=` is on the URL. That id exists **only** in the submit response, so `submitSolution()` now returns it and the submit form carries it across. Protocol confirmed against a live job: send the channel id, then `DOWNLOADED`/`STARTED`/`TASK`.../`FAILED`                                                                                                                                          | `FINISHED`; messages are replayed for 5 minutes, so connecting late loses nothing. Uses `MONITOR_WS_URL`, not core-api's `monitorUrl` -- the latter names `recodex.local`, which this browser cannot resolve. |
| S-017 | Solution source code viewer        | `SolutionSourceCodes` page   | done   | `app/[locale]/(app)/solutions/[solutionId]/sources/page.tsx`, `components/solutions/source-file.tsx`, `lib/api/solution-files.ts`, `app/api/solutions/[solutionId]/download/route.ts`. ZIP entries expand into first-class files the way the legacy app does (`archive.zip#src/main.c`, which is also the key a review comment stores); the legacy display ceiling (32 files, 1 MiB total) is reproduced, with the archive offered instead past it. D-009's viewer was refactored to emit tokens rather than an HTML string so S-018 can put threads between lines (DEC-063), and line anchors are now prefixed per file. The seed now submits a real ZIP (`[seed] zip archive`) so the entry expansion is verified rather than inferred. Diff mode (`react-diff-viewer`) is **not** here -- it is its own ticket. |
| S-018 | Solution review — inline comments  | `solutionReviews` module     | done   | `components/solutions/{reviewable-code,review-comment,review-controls,review-summary}.tsx`, `lib/actions/solution-review.ts`, `lib/api/solution-review.ts`. Comments pin to a line, are added from a per-line gutter button (keyboard-reachable, unlike the legacy double-click, which is kept as well) or double-click, and are edited/deleted in place. Start / mark reviewed / close / reopen / erase reproduce the legacy state machine. **A review stays hidden from its author until it is closed** even though core-api would disclose it (DEC-064) -- verified end to end with two browser contexts in `e2e/solution-sources.spec.ts`.                                                                                                                                                                     |
| S-019 | Solution plagiarism report         | `SolutionPlagiarisms` page   | done   | `app/[locale]/(app)/solutions/[solutionId]/plagiarisms/page.tsx`, `components/solutions/marked-source.tsx`, `lib/api/plagiarism.ts`. Reached from the similarities badge on any solution list and from the solution screen. Teacher-only, by core-api's own hint -- the author of a solution is not told a report exists. Matched passages are marked at their exact character offsets, not by line (DEC-077). The seed uploads one similarity the way a detection tool would (DEC-078), since nothing else can produce this screen's data                                                                                                                                                                                                                                                                         |
| S-020 | Shadow assignment detail           | `ShadowAssignment` page      | done   | `app/[locale]/(app)/shadow-assignments/[shadowId]/page.tsx`, `components/assignments/shadow-points-table.tsx`, `lib/api/shadow-assignment.ts`, `lib/actions/shadow-points.ts`. Nothing is submitted, so nothing offers to; the deadline is stated as informative. A teacher awards, edits and withdraws points here -- the only points in ReCodEx a person types in -- and the group's assignments tab now lists shadow assignments under their own heading, which is how this page is reached. Seeded, since none existed                                                                                                                                                                                                                                                                                         |
| S-021 | User profile                       | `User` page                  | done   | `components/users/profile-view.tsx`, rendered by both `/users/[userId]` and `/profile`, `lib/api/user-profile.ts`. Account rows (present only where core-api discloses them) and the groups the person belongs to. **A user carries no `permissionHints`** (verified live), so the group list is attempted and a 403 read as "not disclosed" (DEC-080). S-022 added the edit control here, T-005 the per-group solutions link, AD-003 will add the takeover                                                                                                                                                                                                                                                                                                                                                        |
| S-022 | User settings (edit profile)       | `EditUser` page              | done   | `app/[locale]/(app)/profile/edit/page.tsx`, `components/users/account-forms.tsx`, `lib/actions/account.ts`, `lib/api/user-settings.ts`. Name and email, password, notification settings, and **the `userCalendars` iCal token manager (Q-014 closed)**. About oneself only -- the id comes from the session, not the URL; an administrator editing somebody else is AD-002's. Changing a password invalidates every token, so that form signs the reader out (DEC-082). Adds the "edit my account" control to S-021's profile                                                                                                                                                                                                                                                                                      |
| S-023 | Accept group invitation            | `AcceptGroupInvitation` page | done   | `app/[locale]/(app)/accept-group-invitation/[invitationId]/page.tsx`, `components/groups/invitation-accept.tsx`, `lib/api/group-invitation.ts`, `lib/actions/group-invitation.ts`. Authenticated (it lives in `(app)`): core-api needs to know who is joining. The button follows `permissionHints.acceptInvitation` rather than the legacy page's role check (DEC-083), and every reason it can be absent -- expired, organizational, archived, already a member -- is stated in words. Five seeded invitation links, one per state. **T-018 is unblocked**                                                                                                                                                                                                                                                       |
| S-024 | Accept invitation (generic)        | `AcceptInvitation` page      | done   | `app/[locale]/(anon)/accept-invitation/page.tsx`, `components/users/accept-invitation-form.tsx`, `app/api/auth/accept-invitation/route.ts`, `lib/auth/invitation-token.ts`. The only screen that creates an account, and the only one reached with no session. The token is the whole query string (core-api's `invitationUrl` template); it is decoded on the server (DEC-084) and verified by nobody here (DEC-085, Q-018). Accepting mints a session, so it is a Route Handler in the auth BFF (DEC-086). Added the `<main>` landmark every `(anon)` page was missing                                                                                                                                                                                                                                           |
| S-025 | Dashboard — shadow assignment rows | `shadowAssignments` module   | done   | `components/dashboard/shadow-assignments.tsx`, `getStudentDashboard()` fans `/v1/groups/{id}/shadow-assignments` over the reader's groups. A table of its own, never rows in the deadline table (DEC-079's reasoning again) and deliberately not in the calendar (DEC-087): the deadline is informative, so it carries no urgency badge and no relative time. Points were always in the group totals — core-api folds them in — this is which ones they are. Seeded a second, unawarded shadow assignment so the "nothing yet" row exists. Fixed a heading-level jump in `StatusState` found on the way                                                                                                                                                                                                            |
| S-026 | Joining and leaving a group        | `groups` module              | done   | `lib/actions/group-membership.ts`, `components/groups/membership-button.tsx`, `ownMembershipAction()` on the group page. **The one screen in this app that reads an ACL's conditions instead of a permission hint, because core-api publishes none for these two actions** (DEC-090) -- `addStudent`/`removeStudent` are absent from a group's hints entirely, since both rules are written against a _student_ subject. The actions take no user id: it comes from the session, and a group's own admins and supervisors are shown neither control -- the legacy screen's own rule. Seeded `[seed] Open Enrolment`, the only joinable group                                                                                                                                                                       |

---

## Anonymous Flows (Phase 4)

| ID    | Title                           | Inventory Rows                 | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ------------------------------- | ------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A-001 | Landing page (Home)             | `Home` page                    | todo   | Public, marketing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| A-002 | Login page                      | `Login` page                   | done   | `app/[locale]/(anon)/login/page.tsx`, `components/auth/login-form.tsx`, `lib/auth/{redirect-target,short-session}.ts`. The app's front door, until now a placeholder in front of a BFF route only the e2e suite called. Posts to that route rather than to a Server Action (brief §5 -- the cookie is the route's job), returns the reader to whatever `proxy.ts` refused them, and **refuses to be talked into leaving the app**: `?from=` is a path here or it is the dashboard. Short sessions are supported where a deployment configures `SHORT_SESSION_MINUTES`, which this one does not, so the checkbox is absent -- the legacy app hides it the same way. **CAS is not offered**: this deployment configures no external authenticator (Q-004), so A-007 owns the button; the callback's failure is rendered here already |
| A-003 | Registration page               | `Registration` page            | todo   | `ALLOW_LOCAL_REGISTRATION`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| A-004 | Forgot password — request       | `ResetPassword` page           | done   | `app/[locale]/(anon)/forgot-password/page.tsx`, `components/auth/forgot-password-form.tsx`, `app/api/auth/forgotten-password/route.ts`. **Answers the same way for a known and an unknown address** (DEC-100): core-api 404s for an unknown login, and passing that through would make the form an account-existence oracle. The sign-in page links here, which is the link it owed since A-002                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A-005 | Forgot password — change        | `ChangePassword` page          | done   | `app/[locale]/(anon)/forgot-password/change/page.tsx`, `components/auth/change-password-form.tsx`, `app/api/auth/forgotten-password/change/route.ts`, `lib/auth/query-token.ts`. The token is the **whole query string** (`readQueryToken`, extracted from S-024's invitation page) and is used as the bearer for exactly one call -- never established as a session, since core-api invalidates it and every other token the moment the password changes. **`proxy.ts` forwards core-api's own `/forgotten-password/change` address here** (DEC-100), so a deployment need not reconfigure `linkTemplates` for its emails to work. Password strength is core-api's zxcvbn through `app/api/auth/password-strength`; a score of zero is refused, as the legacy form refuses it                                                     |
| A-006 | Email verification              | `EmailVerification` page       | todo   | Token from email. Also owns the legacy dashboard's "e-mail not verified" callout (`NotVerifiedEmailCallout`), deliberately not half-built in S-001 — its resend action belongs to this ticket.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A-007 | CAS login external finalization | `LoginExternFinalization` page | todo   | Popup callback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| A-008 | Locale switch cs↔en             | `langMiddleware`               | todo   | Persist in cookie                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

---

## Teacher / Supervisor (Phase 5)

| ID    | Title                                      | Inventory Rows                    | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----- | ------------------------------------------ | --------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-001 | Create assignment from exercise            | `EditAssignment` page             | done   | `app/[locale]/(app)/groups/[groupId]/assign/page.tsx`, `components/assignments/exercise-picker.tsx`, `lib/api/exercises.ts`. Reached from the group's Assignments tab on `assignExercise`. core-api has no call that creates _and_ configures, so assigning lands on T-002's settings with the assignment already real and still invisible (DEC-093). The catalog is searched **server-side** -- it is the one list this app does not fetch whole. **Also added deleting an assignment** to T-002's screen, without which this was a one-way door                                                                                                                                                                                                                                                                                                                                                                        |
| T-002 | Edit assignment                            | `EditAssignment` page             | done   | `app/[locale]/(app)/assignments/[assignmentId]/edit/page.tsx`, `components/assignments/assignment-form.tsx`, `lib/api/assignment-edit.ts`, `lib/actions/assignment.ts`. Visibility, deadlines and points, submission limits, what a student sees of the evaluation, and the per-locale hint. One submit, because `updateDetail` replaces the assignment with what it is sent; `version` is the optimistic lock. Adds the "edit" link to `/assignments/[id]` (DEC-066) **and the re-sync button S-013's notice only reported** (DEC-092). The page checks `update` itself -- reading an assignment is something a student may do. Deleting an assignment lives here too, added by T-001                                                                                                                                                                                                                                   |
| T-003 | Assignment solutions list                  | `AssignmentSolutions` page        | done   | `app/[locale]/(app)/assignments/[assignmentId]/solutions/page.tsx`, `components/assignments/solutions-table.tsx`, `lib/api/assignment-solutions.ts`. One row per **submission**, where S-013's class progress is one row per student -- the same hint (`viewAssignmentSolutions`) gates both. Adds the "all submissions" link to `/assignments/[id]` (DEC-066). Sort and filter are `DataTable`'s, so the view is in the URL                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| T-004 | Assignment stats                           | `stats` module                    | done   | Shipped as part of S-013: the assignment screen's four tiles are exactly this row -- how many of the class have submitted, how many are correct, the average over students whose best solution was **scored** (not over everyone, which would report a class that has not started as one that is failing), and the review requests waiting. Kept as its own row because the teacher phase was written before that screen existed, the same way T-017 was                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-005 | Group user solutions                       | `GroupUserSolutions` page         | done   | `app/[locale]/(app)/groups/[groupId]/users/[userId]/page.tsx`, `lib/api/group-user-solutions.ts`, `closePendingReviews()`, `components/groups/close-pending-reviews.tsx`. One student's whole course, newest first, with an **Assignment** column instead of the legacy screen's group-by-assignments toggle (DEC-094) -- `SolutionsTable` grew a `lead` mode and T-003 renders through the same component. A student may open their own and is refused a classmate's, which is core-api's `viewStudentStats` rule and not a check here. Reached from the roster (staff, or one's own row) and from the user profile (S-021's owed link, now paid). Closing every open review at once is the one write on the screen, and it confirms first                                                                                                                                                                              |
| T-006 | Points overview table                      | `groupResults` module             | done   | `components/groups/points-matrix.tsx`, `getGroupPointsMatrix()`, on the group's Students tab under S-007's roster. Every student against every assignment; both tables come out of the same `/students/stats` response, so the matrix costs one call for the assignment names. **A cell distinguishes "never submitted" from "every attempt failed"** -- one extra `/v1/assignment-solvers?groupId=` for the whole group, and the first thing to close any part of Q-012. Not a `DataTable`: its columns are data, not a schema. Export is T-007                                                                                                                                                                                                                                                                                                                                                                         |
| T-007 | Points export (CSV/Excel)                  | `groupResults` module             | done   | `app/api/groups/[groupId]/points/route.ts`, `getGroupPointsExport()`, `lib/format/csv.ts`. A Route Handler, not a browser-side blob (DEC-095): the link needs no JavaScript, the numbers are re-read at the moment of download, and core-api re-checks the reader. **CSV only** -- semicolons and a BOM, which is what the legacy "Excel" export actually is; a real `.xlsx` would be a new dependency for a capability legacy never had. Shadow assignments **are** columns here (the screen's are not), emails ride along where core-api discloses them, and a quote inside a field is doubled per RFC 4180 rather than backslash-escaped the way the legacy exporter does                                                                                                                                                                                                                                             |
| T-008 | Create/edit exercise                       | `EditExercise` page               | done   | `app/[locale]/(app)/exercises/[exerciseId]/edit/page.tsx`, `components/exercises/{exercise-form,exercise-controls,create-exercise}.tsx`, `lib/actions/exercise.ts`. **Create first, configure second (DEC-098)**, as assigning does: core-api's `actionCreate` takes a group and nothing else, so the reader lands on the settings form with the exercise already real -- and broken by construction, which means nobody can assign it meanwhile. The settings save carries every field and `version` as the optimistic lock (DEC-092's shape). Tags, groups, archiving and deletion are each their own call, so they sit beside the form rather than in it. **Archiving takes `update` away**, so the page is gated on any of update/archive/remove -- found by its own spec, which archived an exercise and was then refused the screen holding the button that undoes it. Files, administrators and forking are T-023 |
| T-009 | Exercise config editor                     | `EditExerciseConfig` page         | todo   | Pipeline config, variables — hardest screen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T-010 | Exercise limits editor                     | `EditExerciseLimits` page         | todo   | Per-environment limits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| T-011 | Reference solutions                        | `ExerciseReferenceSolutions` page | todo   | List, detail, evaluation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-012 | Exercise assignments                       | `ExerciseAssignments` page        | todo   | Assignments from this exercise                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-013 | Pipeline list                              | `Pipelines` page                  | todo   | Search, filter                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-014 | Pipeline detail                            | `Pipeline` page                   | todo   | Structure, boxes, variables                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T-015 | Pipeline editor                            | `EditPipeline` page               | todo   | Edit structure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-016 | Pipeline structure visual editor           | `EditPipelineStructure` page      | todo   | Graphviz — decide approach                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-017 | Group exam locks                           | `groupExamLocks` module           | done   | Shipped as part of S-008: locking and unlocking students, the live roster of who is locked in, and the lock records of a held exam all live on the group's Exams tab. Kept as its own row because the teacher phase was written before that tab existed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T-018 | Group invitations management               | `groupInvitations` module         | done   | `lib/api/group-invitation.ts`, `lib/actions/group-invitation.ts`, `components/groups/invitation-manager.tsx`, on the group's Settings tab. Create, edit and delete; expired links stay listed and marked, because letting one lapse and deleting it are different things. The link is printed in full from the request's own `Host` (DEC-091) rather than guessed from `API_BASE_PUBLIC`. `editInvitations` is granted separately from `update`, so the Settings tab now appears for a reader who may only mint links. Did **not** fully retire `e2e/helpers/core-api.ts`: an organizational group shows no invitation section, so two seeded fixtures remain unreachable by clicking                                                                                                                                                                                                                                    |
| T-019 | Submission failures log                    | `SubmissionFailures` page         | done   | `app/[locale]/(app)/submission-failures/page.tsx`, `lib/api/submission-failures.ts`, `components/failures/failure-table.tsx`, `resolveSubmissionFailure()`. Not a teacher's view after all: the failure ACL is the instance's, and a group supervisor is refused (verified live). **Opens on what is unresolved** (DEC-096) -- core-api serves that list itself and the full history is unbounded and unpaginated. Resolving takes a note and is permanent, so it is a dialog rather than a button; the email to the author is offered and defaults to off. A reference solution's failure names itself but does not link: T-011 has no screen for it yet                                                                                                                                                                                                                                                                |
| T-020 | Exercise catalog                           | `Exercises` page                  | done   | `app/[locale]/(app)/exercises/page.tsx`, `getExerciseCatalog()`, `components/exercises/exercise-table.tsx`, `lib/api/runtime-environments.ts`. **Filed in this session, not by the original plan:** the teacher block jumped from assigning to editing, and `INVENTORY.md` had the catalog and the exercise detail as todo rows nobody owned -- while the sidebar linked to a `PlaceholderPage`. Search, filters, ordering and paging are all core-api's (DEC-097), so a narrowed view is a shareable URL and the screen needs no JavaScript. **Found and fixed a shipped bug on the way**: T-001's picker was passing `?search=`, which core-api ignores (`filters[search]` is the parameter), so its search box did nothing. Creating an exercise is T-008's, with the screen that makes it usable                                                                                                                     |
| T-021 | Exercise detail                            | `Exercise` page                   | done   | `app/[locale]/(app)/exercises/[exerciseId]/page.tsx`, `lib/api/exercise-detail.ts`, `components/exercises/exercise-detail.tsx`, `lib/format/bytes.ts`. What an exercise is, for somebody deciding whether to assign it. **When core-api says an exercise is broken it also says why** -- `validationError` is a list of `@key` reasons, each rendered as a sentence in both locales, which is the most useful thing on the page. Read-only: the tests (T-009), limits (T-010), reference solutions (T-011) and the assignments made from it (T-012) are named but not linked, because none of those screens exists yet (DEC-066); the assignment **count** is here, since it is what says the exercise is in use                                                                                                                                                                                                         |
| T-022 | Discussion threads                         | `comments` module                 | todo   | **Filed in this session.** The legacy app puts a comment thread on four screens -- the exercise, the assignment, the assignment's solutions and a solution's sources (`CommentThreadContainer`, `/v1/comments/{threadId}` with per-comment private/public toggling) -- and nothing here has built any of them. Not the same thing as S-018's inline review comments, which is what `INVENTORY.md`'s `comments` row wrongly pointed at; that row is corrected. S-012, S-013, S-015 and S-017 all shipped without it                                                                                                                                                                                                                                                                                                                                                                                                       |
| T-023 | Exercise files, administrators and forking | `EditExercise` page               | todo   | **Filed with T-008**, which deliberately stopped at basic settings: the exercise's own files and their link keys (`/exercises/{id}/files`, `/file-links`, and `localizedTextsLinks`, which the text renderer does not substitute yet -- assignments have the same gap), its administrators (`/admins`, `changeAuthor`), and forking an exercise into another group (`/fork`). All three are legacy capabilities with no home yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

---

## Admin (Phase 6)

| ID     | Title             | Inventory Rows          | Status | Notes                                                                                                                       |
| ------ | ----------------- | ----------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------- |
| AD-001 | User list         | `Users` page            | todo   | Search, filter, edit                                                                                                        |
| AD-002 | User detail       | `User` page             | todo   | Info, groups, solutions, takeover. Owns editing _another_ user's account; S-022 built the same forms for oneself            |
| AD-003 | User takeover     | `userSwitching` module  | todo   | Superadmin switch to user. The BFF route already exists (F-020); this ticket adds the button, on the profile screen (S-021) |
| AD-004 | Instance list     | `Instances` page        | todo   | CRUD                                                                                                                        |
| AD-005 | Instance edit     | `EditInstance` page     | todo   | Settings, limits                                                                                                            |
| AD-006 | Server management | `ServerManagement` page | todo   | Runtime environments, hardware groups                                                                                       |
| AD-007 | System messages   | `SystemMessages` page   | todo   | Broadcast to users                                                                                                          |
| AD-008 | Licences          | `licences` module       | todo   | CRUD                                                                                                                        |

---

## Parity Sweep & Polish (Phase 7)

| ID    | Title                                     | Inventory Rows | Status | Notes                                  |
| ----- | ----------------------------------------- | -------------- | ------ | -------------------------------------- |
| P-001 | Feature parity check                      | All rows       | todo   | Walk INVENTORY.md top to bottom        |
| P-002 | Accessibility pass                        | All pages      | todo   | Keyboard, focus, ARIA, contrast        |
| P-003 | Performance pass                          | All pages      | todo   | Bundle size, waterfalls, over-fetching |
| P-004 | cs/en review                              | All strings    | todo   | Native speaker review                  |
| P-005 | README + deployment notes                 | —              | todo   | Fresh developer can start              |
| P-006 | `docs/ROUTES.md` — old→new mapping        | All routes     | todo   | For redirect planning                  |
| P-007 | `docs/DROPPED.md` — intentional omissions | Dropped items  | todo   | With justification                     |
| P-008 | `docs/RETROSPECTIVE.md`                   | —              | todo   | §13 — cited from notes                 |

---

## Current Focus

| ID    | Title             | Status |
| ----- | ----------------- | ------ |
| A-003 | Registration page | todo   |

---

## Blocked

| ID  | Title | Reason |
| --- | ----- | ------ |
| —   | —     | —      |

---

## Dropped

| ID  | Title | Reason           |
| --- | ----- | ---------------- |
| —   | —     | See `DROPPED.md` |
