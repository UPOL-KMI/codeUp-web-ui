# ReCodEx New Frontend — Backlog

**Status:** Draft
**Date:** 2026-05-11

---

## Legend

| Status    | Meaning                                        |
| --------- | ---------------------------------------------- |
| `todo`    | Ready to work                                  |
| `doing`   | Currently in progress                          |
| `done`    | Completed and verified                         |
| `blocked` | Cannot proceed — reason recorded               |
| `dropped` | Intentionally omitted — reason in `DROPPED.md` |

---

## Foundation (Phase 1)

| ID    | Title                                                       | Inventory Rows   | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ----------------------------------------------------------- | ---------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | Scaffold Next.js 16.3 repo with App Router, TS strict, pnpm | —                | done   | 16.3.1 confirmed latest via `npm view`. `typecheck`/`lint`/`build` all green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-002 | ESLint flat config + Prettier + TypeScript 7                | —                | done   | **TypeScript pinned to 6.0.3, not 7** — `typescript-eslint`/`eslint-plugin-react` don't support TS7/ESLint10 yet, see AGENTS.md "Toolchain deviations". `pnpm typecheck`/`lint` both work standalone; no pre-commit hook wired yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| F-003 | Dockerfile with `output: 'standalone'`                      | —                | done   | Lives in this repo's own root (`Dockerfile`), not `services/<name>/` — this repo isn't one of the pull-repos.sh-managed upstream ReCodEx repos, so it doesn't share their build-context pattern; see DECISIONS.md. `docker build` + `docker run` both verified working end to end, real HTTP 200 with rendered HTML.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| F-004 | Compose service entry (self-contained add)                  | —                | done   | Diff shown, operator approved as-is. `web-next` service added to `../ReCOdex/docker-compose.yaml` (DEC-028); `WEB_NEXT_PORT` added to `../ReCOdex/.env`/`.env.example`. Verified live: `docker compose up -d web-next`, HTTP 200, and `api` reachable internally via `getent hosts api`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F-005 | CI pipeline (GitHub Actions)                                | —                | done   | `.github/workflows/ci.yml`: `typecheck`, `lint`, `build`, `test`, via `corepack enable` (matches Dockerfile, not `actions/setup-pnpm`). Added `vitest.config.ts` (`passWithNoTests: true` — no tests exist yet, F-023/F-024 land the harness) and `"type": "module"` to `package.json` (silenced a real Vite config-loader warning, not just cosmetic). All four steps verified locally, including `build` with `.env.local` removed to simulate CI's actual environment. **Gap found and fixed during F-010:** `format:check` existed as a script since F-002 but was never added to this pipeline, and the whole repo had never actually been run through Prettier — `pnpm format:check` failed on 15 files, none touched by F-010 itself. Fixed with `pnpm format` (repo-wide reformat, whitespace/table-alignment only, no logic changes) and added `format:check` as a fifth CI step.                                                                                        |
| F-006 | Runtime config: `.env.local` + `.env.example`               | `config.js`      | done   | `API_BASE_INTERNAL`/`API_BASE_PUBLIC`/`MONITOR_WS_URL` set to verified real values (docs/QUESTIONS.md Q-001/Q-006), not placeholders. CAS/SMTP vars deferred to their own auth tickets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F-007 | `next.config.ts`: `basePath` from build arg                 | `config.js`      | done   | Already landed as part of F-003: `Dockerfile`'s `ARG URL_PATH_PREFIX` + `next.config.ts`'s `basePath`. Build-time limitation documented in `DROPPED.md`. Noticed and closed out while updating this table for F-025, not separately implemented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F-008 | Turbopack filesystem cache for builds                       | —                | done   | `turbopackFileSystemCacheForBuild` defaults to `true` (confirmed in `node_modules/next/dist/server/config-shared.d.ts`'s default-config export) — no `next.config.ts` change needed. Verified for real, not just from the default: `.next/cache/turbopack/v16.3.1-*/` is populated with real cache files (SST/LOG/CURRENT) after a build, and a warm second build compiles in 254ms vs. 1673ms cold — ~6.5x.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| F-009 | Turbopack memory eviction in dev                            | —                | done   | `turbopackMemoryEviction` defaults to `'auto'` (`config-shared.d.ts`) -- no config change needed. Verified with a real (if short) `next dev` session: RSS dropped from ~457MB to ~351MB (23%) over 60s idle with zero requests in between, consistent with active eviction. A fuller "does memory stay bounded over hours of real use" test isn't meaningful yet -- this repo only has 2 placeholder routes, nothing substantial to evict. Revisit once real route/module volume exists (Design System phase onward).                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-010 | Theme tokens (dark/light)                                   | `config.js` SKIN | done   | Tailwind CSS v4 (not previously installed — brought in as part of this ticket) + shadcn/ui-compatible CSS custom properties in `app/globals.css` (`:root`/`.dark`, `oklch`, mapped via `@theme inline`) + `next-themes` for the class toggle. See DEC-032. Verified via compiled build output (`.next/static/chunks/*.css` has both blocks with genuinely different values; `body`/`*` rules correctly resolve through the custom properties) and the rendered HTML of a real `next start` (next-themes' anti-FOUC script present and correctly configured). No component-level browser click-through yet — nothing to click on besides the placeholder page; full visual verification belongs with the first real UI ticket (D-series).                                                                                                                                                                                                                                          |
| F-011 | i18n: next-intl with cs/en                                  | `locales/`       | done   | `next-intl@4.13.7`, `app/[locale]/...`, `localePrefix: "always"`. Restructured `app/layout.tsx`/`app/page.tsx` into `app/[locale]/layout.tsx`/`page.tsx` (the `[locale]` layout _is_ the root layout now). `proxy.ts` (not `middleware.ts`) wraps `next-intl/middleware`. `messages/{en,cs}.json` started minimal (just the placeholder page) — full legacy message migration happens per-screen as S-/T-/D-series tickets land, not as a bulk port. See DEC-033. Verified via `next build`, a live `next dev` session, and a real `docker build`+`docker run` (proxy/middleware bundling under `output: standalone` has a history of subtle breakage, so didn't trust `next dev` alone).                                                                                                                                                                                                                                                                                         |
| F-012 | Error/not-found/forbidden conventions                       | `NotFound` page  | done   | `app/[locale]/{error,not-found,forbidden,unauthorized}.tsx` + `app/{global-error,global-not-found}.tsx`. `forbidden()`/`unauthorized()` need `experimental.authInterrupts`; the bogus-URL-under-valid-locale case needed `experimental.globalNotFound` too (found live, not from docs — see DEC-034). `catchError` turned out unnecessary for route-segment boundaries; standard `error.tsx`'s `retry` prop (stable since 16.3.0) already covers it. Verified via temporary test routes hitting all four states (403/401/500/404) for real HTTP status + rendered content, in `next dev` and a full `docker build`+`run`, then deleted before committing.                                                                                                                                                                                                                                                                                                                         |
| F-013 | Route skeleton with layouts                                 | All pages        | done   | `(anon)`/`(app)` route groups under `app/[locale]/...`. All 16 top-level static routes from `docs/IA.md`'s sitemap stubbed with a shared `PlaceholderPage`, titles sourced from the legacy app's real translations, not guessed. Dynamic sub-routes (`/groups/[groupId]`, etc.) deliberately deferred to their owning S-/T-/A-/AD- ticket. See DEC-035. Verified via `next build`'s route list, a live `next dev` session, and a full `docker build`+`run`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| F-014 | `proxy.ts` for auth UX redirect                             | `auth` module    | done   | Presence-only session-cookie check (never validates — that's F-015's job). Composed with next-intl's own middleware from F-011 by short-circuiting on its redirects. Three-way pathname split (auth-only, public, everything-else) kept in sync by hand with F-013's route groups. `lib/auth/session-cookie.ts` holds the shared cookie-name constant F-016 will reuse. See DEC-036. Verified all six presence/pathname combinations plus the chained locale→auth redirect with real requests, in `next dev` and a full `docker build`+`run`.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-015 | `requireSession()` server-side guard                        | `auth` module    | done   | `lib/auth/require-session.ts`. Decodes the session cookie's JWT payload (no library, no signature verification -- core-api is the real authority), checks `exp`, redirects to `/login` on failure. Not wired into `(app)/layout.tsx` -- brief §5's explicit "not the layout," each data-touching function guards itself once F-022 lands. See DEC-037. Verified live via a temporary test route (deleted before commit): no cookie, a real valid JWT from an actual login, an expired token, and a malformed cookie -- all four behave correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| F-016 | Auth BFF: login Route Handler                               | `auth` module    | done   | `app/api/auth/login/route.ts`. `{email, password}` -> core-api `username`/`password`; `sessionCookieOptions()` (`lib/auth/session-cookie.ts`) sets httpOnly/sameSite=lax/`secure` derived from `API_BASE_PUBLIC`'s scheme, `maxAge` from the JWT's real `exp`. Corrects DEC-023 (login is a Route Handler, not a Server Action -- brief §5's own words). See DEC-038. Verified for real: correct/wrong credentials, malformed input, and the full login-cookie-`proxy.ts`-`requireSession()` round trip, in both `next dev` and the production compose container over the internal Docker network.                                                                                                                                                                                                                                                                                                                                                                                |
| F-017 | Auth BFF: logout Route Handler                              | `auth` module    | done   | `app/api/auth/logout/route.ts`. POST only, clears the session cookie, 303s to `/login`. core-api has no logout endpoint (confirmed against swagger) -- BFF-side only. Found and fixed a real bug along the way: `request.url` is unreliable under `output: standalone` (reflects the container's internal bind address, not the actual `Host`) -- see DEC-039. Verified the full login-then-logout round trip in both `next dev` and the production compose container.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F-018 | Auth BFF: token refresh in `proxy.ts`                       | `auth` module    | done   | `lib/auth/refresh-session.ts`'s `maybeRefreshSession()`. Proactive refresh 24h before `exp`, in-memory per-token de-duplication (module-scope `Map`). Found live: core-api doesn't invalidate the old token on refresh (changes the failure mode from "invalidate each other" to "redundant call"), and its JWTs are second-granularity deterministic (a real gotcha for testing de-duplication by comparing token values). See DEC-040. Verified by counting actual upstream calls: 6 concurrent requests sharing one soon-to-expire cookie produced exactly 1 refresh call.                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-019 | Auth BFF: CAS callback Route Handler                        | `auth` module    | done   | `app/api/auth/external/[authenticatorName]/callback/route.ts`. Generic dynamic segment, not hardcoded to CAS -- reads a `token` query param, POSTs to core-api's `/login/{authenticatorName}`, reuses F-016's `establishSession()`. Mechanism resolved by reading core-api's own PHP source, not guessed; see DEC-041. Verified live (both `next dev` and Docker `standalone`) that `request.url`'s query string is safe to read despite DEC-039's host-portion bug. Success path against a real external provider is unverifiable here -- no `EXTERNAL_AUTH_*` configured (`QUESTIONS.md` Q-004).                                                                                                                                                                                                                                                                                                                                                                                |
| F-020 | Auth BFF: user takeover                                     | `auth` module    | done   | `app/api/auth/takeover/[userId]/route.ts`, mirrors core-api's `/login/takeover/{userId}` 1:1. Authorization fully delegated to core-api's `canTakeOver()`/`permissions.neon` (superadmin-only, confirmed by reading the neon config) -- this route only checks a session exists, forwards it, and surfaces core-api's 403/404 as JSON. Verified live (dev + Docker) against real seeded accounts: superadmin takeover succeeds and swaps the session's `sub`; non-superadmin gets 403; no session gets 401; malformed `userId` gets 400. See DEC-042.                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-021 | Auth BFF: restricted token generation                       | `auth` module    | done   | `app/api/auth/restricted-token/route.ts`. Self-service "Generate Application Token" primitive (legacy `GenerateTokenForm.js`) -- deliberately returns the raw token in the JSON response (the one intentional exception to DEC-021) and never touches the session cookie. Scoped narrowly to this primitive, not the separate `restrictEffectiveRole()` session-swap UX also found in the legacy app; see DEC-043. Verified live (dev + Docker): scoped token decodes to exactly the requested scopes, caller's session cookie unchanged; core-api's own validation errors (forbidden scope, unknown role, expiration cap) forwarded verbatim.                                                                                                                                                                                                                                                                                                                                    |
| F-022 | Typed `server-only` API client                              | `apiMiddleware`  | done   | `lib/api/client.ts` + generated `lib/api/core-api.generated.ts` (`pnpm generate:api-types`, vendored spec `openapi/core-api.yaml`). `path` typed against `keyof paths` -- real compile-time endpoint checking -- but response types stay hand-written: core-api's swagger has zero response schemas (confirmed: ~250 "Placeholder response" entries, no `components.schemas`), so `openapi-fetch`'s typed-response value doesn't apply here. `requireSession()` on every call, `cache: "no-store"` always, `ApiError` normalizes core-api's `{success, error, payload}` envelope using the same short `code` strings (`"403-002"`) the legacy app's own `apiErrorMessages.js` keys on. Found and fixed a real double-`/v1`-prefix URL bug live (both `API_BASE_INTERNAL` and every generated path key include `/v1`). See DEC-044. Verified live (dev + Docker) via a temporary debug route: real `GET /v1/users/{id}` success and a genuine core-api 404 both surface correctly. |
| F-023 | Playwright harness skeleton                                 | —                | done   | `playwright.config.ts` + `e2e/`. Logs in via the real Auth BFF login route (no form exists yet), runs against `next build`+`next start` (not dev), Chromium only, `workers: 2` (found live: default worker count overwhelms the local dev core-api's PHP-FPM pool with concurrent bcrypt logins). Skipped `@next/playwright` -- its `instant()` helper requires Cache Components, which this repo deliberately has off (DEF-001). Not wired into CI (no reachable core-api there). See DEC-045 for the 3 real bugs found while building this (private-folder routing, `test.use({storageState})` describe-scope leakage, worker concurrency). 48/48 tests pass stably: 8 public routes + 4 seeded personas x 10 protected routes each.                                                                                                                                                                                                                                            |
| F-024 | Token-leakage security test                                 | —                | done   | `e2e/security.spec.ts`, brief §8's "non-negotiable" test. Uses real `page.request.post()` login (not F-023's `context.addCookies()` shortcut, which would make the httpOnly check tautological) so Playwright parses the actual `Set-Cookie` header; finds the session cookie by diffing `context.cookies()` rather than assuming its name. Scans every protected route's full HTML plus every served JS bundle for the raw token. Deliberately verified both assertions can actually fail (temporarily flipped each, confirmed a real failure, reverted) before trusting either. See DEC-046.                                                                                                                                                                                                                                                                                                                                                                                    |
| F-025 | `scripts/seed.ts` — test data generator                     | —                | done   | API-only, idempotent (verified: a clean re-run against fully-seeded state produces zero creates), `[seed]` prefix. Builds 4 groups, 3 named users + 25 pagination-filler students, 1 fully-configured gradeable exercise reused across 27 assignments, and 2 real submissions. See DEC-029/030/031 for the exercise-build recipe and the cgroup v1 evaluation caveat.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F-026 | `docs/SEED_ACCOUNTS.md`                                     | —                | done   | Rewritten to match what the script actually builds (the recon-phase plan used different group/account names).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## Design System (Phase 2)

| ID    | Title                                               | Inventory Rows      | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | --------------------------------------------------- | ------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-001 | `PageShell` component                               | All pages           | done   | `components/page-shell.tsx`, per `docs/IA.md` §3.3. Plain Server Component -- `breadcrumbs` is required but unresolved (D-002's job); `actions`/`tabs` are pre-built `ReactNode` slots. Wired into `components/placeholder-page.tsx` (all 18 current routes) with a single unlinked crumb as an immediate live-verification path. See DEC-047. Verified live (dev + Docker): full 50-test E2E suite passes unchanged; a temporary debug route confirmed `subtitle`/`actions`/`tabs`/linked-crumb all render correctly, including a correctly locale-prefixed `href`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-002 | Central breadcrumb manifest                         | All pages           | done   | `lib/breadcrumbs/manifest.ts`. Flat `{namespace, pattern}`/`{pattern, resolve}` registry, prefix-walked per pathname, throws on any unregistered route (confirmed live: clean 500, not a silent gap). Nested dynamic segments (`/groups/[groupId]` etc.) deliberately unregistered -- no page exists yet to test a resolver against; type signature ready for whichever ticket builds that route. Wired into `components/placeholder-page.tsx`, simplifying all 17 stub `page.tsx` files (namespace-only prop, no longer `async`). See DEC-048. Verified live (dev + Docker): two-segment `/forgot-password/change` renders a correct two-crumb chain with a properly locale-prefixed link; full 50-test E2E suite passes unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-003 | `DataTable` component                               | All list views      | done   | `components/data-table.tsx`. Client-side sort/filter/paginate over a caller-supplied dataset (legacy `SortableTable`'s own model, read first), but URL-synced (`${id}-sort/-q/-page`) instead of legacy's `localStorage` -- a deliberate correction. Selection stays local state, not URL-synced. Two real bugs found live: `columns` (function props) can't cross the Server-> Client boundary as a prop (documented prominently); a Docker prerender-time failure (`useSearchParams()` needs `Suspense`) that `next dev` never caught -- fixed by wrapping internally so every future caller is unaffected. See DEC-049. Verified live (dev + Docker, specifically re-verified against Docker after the Suspense fix): sort/paginate/filter compose correctly together; selection never touches the URL. **Note (DEC-050)**: brief §4 names TanStack Table as the intended library; investigated its actual v9 API afterward and found a substantial headless/feature-registration rewrite that wouldn't reduce this component's URL-sync work at all -- documented as a verified, deliberate non-adoption, not an oversight.                                                                                                                                    |
| D-004 | Form kit on Server Actions                          | All forms           | done   | `lib/forms/{action-result,use-server-action-form,use-dirty-guard}.ts`, `components/form/{text-field,form-error}.tsx`. RHF's `onSubmit` calls the Server Action directly (not `<form action>` -- the two submission models would conflict), wrapped in `startTransition`. Server errors map onto RHF's own `setError`/`root` error model. `useDirtyGuard` covers `beforeunload` only -- checked bundled docs, confirmed no in-app navigation-blocking primitive exists in the App Router as of 16.3. Real bug found live: a Zod schema co-located in the "use server" file broke at runtime (only function exports survive that compiler pass) -- fixed by splitting the schema into its own shared module. See DEC-051. Verified live (dev + Docker) via a real demo Server Action: client validation, server field error, server form-level error, success, and the dirty-guard state all confirmed working.                                                                                                                                                                                                                                                                                                                                                      |
| D-005 | Upload component (Route Handler)                    | `upload` module     | done   | `lib/upload/{limits,chunked-upload,use-file-upload}.ts`, `components/upload/file-upload.tsx`, `app/api/upload/partial/route.ts`, `app/api/upload/partial/[id]/route.ts`, `app/api/upload/[id]/digest/route.ts`. Brief §6.7's open question answered: the legacy app **does** chunk, via core-api's per-partes protocol (start / `PUT ?offset=N` per chunk / complete / SHA-1 digest check / cancel) with adaptive chunk sizing (64 KiB start, ×2 under 2 s, ÷2 over 4 s, clamped 4 KiB–4 MiB) -- reproduced exactly. Chunk loop stays in the browser so progress, cancel and the checksum check are real; each chunk is proxied through a Route Handler that attaches the session token server-side and forwards the body as an unbuffered stream. 512 MiB ceiling verified against the deployment's own nginx/PHP config, enforced in three layers. Verified live against the running stack (bare host _and_ the Docker `standalone` image): a 300 KiB file uploaded in 5 chunks with core-api's SHA-1 matching the local file byte for byte, plus 401/oversize/bad-filename/bad-uuid/offset-mismatch/cancel/complete-after-cancel all rejecting correctly, and a browser run of the real component showing progress, cancel and the uploaded state. See DEC-053. |
| D-006 | Dialog/modal system                                 | Various             | done   | `components/dialog/{dialog,confirm-dialog}.tsx`, `Dialog` strings in both locales, dialog motion tokens in `app/globals.css`. On the unified `radix-ui` package (1.6.7) rather than per-primitive packages -- more primitives are coming (D-007, D-015, dropdowns/tooltips) and one version that moves together beats a dozen drifting ranges. `ConfirmDialog` is built on `AlertDialog`, not `Dialog`: `role="alertdialog"`, initial focus on Cancel, and no outside-click dismissal, all verified in the installed source. Two stale-by-a-version details checked rather than copied from memory: 1.1.23 omits `aria-describedby` on its own when no description is rendered (the `aria-describedby={undefined}` workaround is obsolete), and it ships no console warnings at all, so a missing title now fails silently -- hence `title` being a required prop. Motion respects `prefers-reduced-motion`. Verified in a real browser: focus trap, Escape, focus restored to the trigger, accessible name/description, backdrop click ignored by the confirm dialog, Cancel focused first. One real bug found by looking at a screenshot -- see DEC-054.                                                                                                         |
| D-007 | Toast notification system                           | Various             | done   | `components/toast/toast-provider.tsx`, mounted once in `app/[locale]/layout.tsx`, reached via `useToast()`. On Radix `Toast` (same family as D-006, DEC-054) for its `role="status"`/`aria-live` handling, hover- and blur-paused timers, swipe dismissal and the F8 focus hotkey. Errors are `foreground` (assertive, 8 s), successes `background` (polite, 4 s). `useToast()` throws outside the provider rather than no-op'ing -- a toast that silently does nothing is the exact failure brief §9 forbids.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-008 | State components (loading, empty, error, forbidden) | All lists           | done   | `components/state/{status-state,skeleton,empty-state,error-state,error-boundary}.tsx`. One `StatusState` layout behind not-found/forbidden/unauthorized/empty/error so they cannot drift apart; those four route pages rewritten onto it. `loading.tsx` added at each route-group level (covers every nested segment). `ErrorBoundary` wraps `catchError` from `next/error` -- verified present in the installed next@16.3.1 -- for panel-level failures, with Next's own `retry()` rather than a bespoke refetch. Skeletons hold still under `prefers-reduced-motion`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-009 | Code viewer with line anchoring                     | `solutions/[id]`    | done   | `components/code/code-viewer.tsx`, `lib/code/{highlight,languages}.ts`. Shiki 4.4.3, server-side only -- no highlighter, grammar or theme JSON reaches the browser, and the code is in the initial HTML (readable pre-hydration, findable with the browser's own Ctrl+F). Extension→language map ported from the legacy `syntaxHighlighting.js` so files look the same as students are used to. Every line gets a stable `id` (`L12`) and a real anchor; `:target` highlights the linked line with no JS. Dual theme via `defaultColor: false` + CSS keyed on next-themes' `.dark`. Files over 512 KiB render unhighlighted and say so. Per-line review comments and collapsed regions stay with the solution-review ticket -- the `data-line` hook they need is here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-010 | Markdown renderer                                   | `exercises/[id]`    | todo   | react-markdown + KaTeX, test compatibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-011 | Status badges                                       | All status displays | todo   | Deadline, evaluation, permission                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-012 | Formatters (date, points, relative time)            | All pages           | todo   | Single source, client/server consistent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-013 | `/dev/kitchen-sink` route                           | —                   | done   | `app/[locale]/dev/design-system/page.tsx` (Server Component) + `components/dev/design-system.tsx` (the one `"use client"` island). Renders theme tokens, `DataTable`, the form kit's error states, both dialog kinds and the upload component. Public in `proxy.ts` -- it shows no user data, and requiring a session would only make it harder to look at; the upload section does hit core-api and says so. Taken ahead of D-007 at the operator's request for a visible page to watch progress on. Route renamed from this ticket's original `/dev/kitchen-sink` at the operator's request -- see PROGRESS.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F-027 | Dependency automation (Dependabot)                  | —                   | done   | `.github/dependabot.yml`. Brief §4 requires this "on day one" with `next` and `react-server-dom-*` grouped; it was missed during Foundation and found while reviewing the repo during D-009 -- it appears in no doc, so it was an oversight, not a deferral. Dependabot over Renovate because Renovate needs a GitHub App installed on the account, which is the operator's call to make, not this repo's.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-016 | Design-system regression spec                       | —                   | done   | `e2e/design-system.spec.ts`. D-003..D-009 each built a throwaway demo page, verified by hand and deleted it, leaving nothing that would catch a regression. Now that D-013's showcase is permanent and public, one spec covers dialogs, the confirm dialog's no-outside-dismiss guarantee, toasts, the error boundary's containment and recovery, and server-side highlighting with working line anchors -- with no login, so it costs none of the bcrypt round trips `playwright.config.ts` had to cut workers for.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-014 | Sidebar / app-shell navigation                      | `(app)/layout.tsx`  | todo   | **Gap found during D-001** (docs/IA.md §3.1 was never given its own ticket): collapsible sidebar, context-aware sections (Dashboard/Exercises/People always visible; My Groups/My Teaching/Admin conditional on per-group membership arrays and role, not the global role). "My Groups"/"My Teaching" need real group-membership API data no earlier ticket fetches yet -- likely needs sequencing after a groups-listing ticket lands, or a minimal own-memberships call added here. `(app)/layout.tsx` is currently still F-013's bare passthrough `<div>`, explicitly deferred to "D-series" without naming a specific ticket.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-015 | Command palette (Cmd/Ctrl-K)                        | `search` module     | todo   | Per `docs/IA.md` §3.4, also found ungapped during D-001: jump to groups/assignments/exercises/users, `/api/search` (or equivalent) with debounced queries. Needs a real search endpoint on core-api's side confirmed before building the client half.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

---

## Student Experience (Phase 3)

| ID    | Title                              | Inventory Rows               | Status | Notes                                       |
| ----- | ---------------------------------- | ---------------------------- | ------ | ------------------------------------------- |
| S-001 | Dashboard — student section        | `Dashboard` page             | todo   | Deadlines, submissions, progress            |
| S-002 | Dashboard — teacher section        | `Dashboard` page             | todo   | Unreviewed, upcoming, activity              |
| S-003 | Dashboard — calendar view          | `userCalendars` module       | todo   | Full month, deadline markers                |
| S-004 | Group list                         | `groups` module              | todo   | My groups + discover                        |
| S-005 | Group detail — Info tab            | `GroupInfo` page             | todo   | Description, stats, announcements           |
| S-006 | Group detail — Assignments tab     | `GroupAssignments` page      | todo   | Filter: all, open, closed, w/ submissions   |
| S-007 | Group detail — Students tab        | `GroupStudents` page         | todo   | Roster, points overview                     |
| S-008 | Group detail — Exams tab           | `GroupExams` page            | todo   | Exam mode management                        |
| S-009 | Group detail — Settings tab        | `EditGroup` page             | todo   | Edit, members, invitations                  |
| S-010 | Group hierarchy (nested subgroups) | `groups` module              | todo   | Tree navigation, breadcrumbs                |
| S-011 | Archived groups view               | `Archive` page               | todo   | Filter, unarchive action                    |
| S-012 | Assignment detail — student view   | `Assignment` page            | todo   | Description, deadlines, my submissions      |
| S-013 | Assignment detail — teacher view   | `Assignment` page            | todo   | Stats, edit link, all submissions           |
| S-014 | Submit solution flow               | `assignment` routes          | todo   | File upload, validation, confirmation       |
| S-015 | Evaluation result display          | `Solution` page              | todo   | Test-by-test, compilation log, judge output |
| S-016 | Live evaluation progress           | `evaluationProgress` module  | todo   | Polling or WebSocket                        |
| S-017 | Solution source code viewer        | `SolutionSourceCodes` page   | todo   | Shiki, diff, line comments                  |
| S-018 | Solution review — inline comments  | `solutionReviews` module     | todo   | Add/edit/delete comments, close review      |
| S-019 | Solution plagiarism report         | `SolutionPlagiarisms` page   | todo   | Detection results, links                    |
| S-020 | Shadow assignment detail           | `ShadowAssignment` page      | todo   | Bonus assignment, points                    |
| S-021 | User profile                       | `User` page                  | todo   | Info, groups, solutions, settings           |
| S-022 | User settings (edit profile)       | `EditUser` page              | todo   | Name, email, password change                |
| S-023 | Accept group invitation            | `AcceptGroupInvitation` page | todo   | Token from email                            |
| S-024 | Accept invitation (generic)        | `AcceptInvitation` page      | todo   | Token from email                            |

---

## Anonymous Flows (Phase 4)

| ID    | Title                           | Inventory Rows                 | Status | Notes                      |
| ----- | ------------------------------- | ------------------------------ | ------ | -------------------------- |
| A-001 | Landing page (Home)             | `Home` page                    | todo   | Public, marketing          |
| A-002 | Login page                      | `Login` page                   | todo   | Local + CAS                |
| A-003 | Registration page               | `Registration` page            | todo   | `ALLOW_LOCAL_REGISTRATION` |
| A-004 | Forgot password — request       | `ResetPassword` page           | todo   | Email sent                 |
| A-005 | Forgot password — change        | `ChangePassword` page          | todo   | Token from email           |
| A-006 | Email verification              | `EmailVerification` page       | todo   | Token from email           |
| A-007 | CAS login external finalization | `LoginExternFinalization` page | todo   | Popup callback             |
| A-008 | Locale switch cs↔en             | `langMiddleware`               | todo   | Persist in cookie          |

---

## Teacher / Supervisor (Phase 5)

| ID    | Title                            | Inventory Rows                    | Status | Notes                                       |
| ----- | -------------------------------- | --------------------------------- | ------ | ------------------------------------------- |
| T-001 | Create assignment from exercise  | `EditAssignment` page             | todo   | Select exercise, deadlines, points          |
| T-002 | Edit assignment                  | `EditAssignment` page             | todo   | Deadlines, points, limits, visibility       |
| T-003 | Assignment solutions list        | `AssignmentSolutions` page        | todo   | All submissions, filter, sort               |
| T-004 | Assignment stats                 | `stats` module                    | todo   | Submitted count, average score              |
| T-005 | Group user solutions             | `GroupUserSolutions` page         | todo   | Per-student drill-down                      |
| T-006 | Points overview table            | `groupResults` module             | todo   | Sortable, exportable                        |
| T-007 | Points export (CSV/Excel)        | `groupResults` module             | todo   | Download endpoint                           |
| T-008 | Create/edit exercise             | `EditExercise` page               | todo   | Basic settings, description                 |
| T-009 | Exercise config editor           | `EditExerciseConfig` page         | todo   | Pipeline config, variables — hardest screen |
| T-010 | Exercise limits editor           | `EditExerciseLimits` page         | todo   | Per-environment limits                      |
| T-011 | Reference solutions              | `ExerciseReferenceSolutions` page | todo   | List, detail, evaluation                    |
| T-012 | Exercise assignments             | `ExerciseAssignments` page        | todo   | Assignments from this exercise              |
| T-013 | Pipeline list                    | `Pipelines` page                  | todo   | Search, filter                              |
| T-014 | Pipeline detail                  | `Pipeline` page                   | todo   | Structure, boxes, variables                 |
| T-015 | Pipeline editor                  | `EditPipeline` page               | todo   | Edit structure                              |
| T-016 | Pipeline structure visual editor | `EditPipelineStructure` page      | todo   | Graphviz — decide approach                  |
| T-017 | Group exam locks                 | `groupExamLocks` module           | todo   | Lock/unlock for exam mode                   |
| T-018 | Group invitations management     | `groupInvitations` module         | todo   | Send, revoke, list                          |
| T-019 | Submission failures log          | `SubmissionFailures` page         | todo   | Admin/teacher view                          |

---

## Admin (Phase 6)

| ID     | Title             | Inventory Rows          | Status | Notes                                 |
| ------ | ----------------- | ----------------------- | ------ | ------------------------------------- |
| AD-001 | User list         | `Users` page            | todo   | Search, filter, edit                  |
| AD-002 | User detail       | `User` page             | todo   | Info, groups, solutions, takeover     |
| AD-003 | User takeover     | `userSwitching` module  | todo   | Superadmin switch to user             |
| AD-004 | Instance list     | `Instances` page        | todo   | CRUD                                  |
| AD-005 | Instance edit     | `EditInstance` page     | todo   | Settings, limits                      |
| AD-006 | Server management | `ServerManagement` page | todo   | Runtime environments, hardware groups |
| AD-007 | System messages   | `SystemMessages` page   | todo   | Broadcast to users                    |
| AD-008 | Licences          | `licences` module       | todo   | CRUD                                  |

---

## Parity Sweep & Polish (Phase 7)

| ID    | Title                                     | Inventory Rows | Status | Notes                                  |
| ----- | ----------------------------------------- | -------------- | ------ | -------------------------------------- |
| P-001 | Feature parity check                      | All rows       | todo   | Walk INVENTORY.md top to bottom        |
| P-002 | Accessibility pass                        | All pages      | todo   | Keyboard, focus, ARIA, contrast        |
| P-003 | Performance pass                          | All pages      | todo   | Bundle size, waterfalls, over-fetching |
| P-004 | cs/en review                              | All strings    | todo   | Native speaker review                  |
| P-005 | README + deployment notes                 | —              | todo   | Fresh developer can start              |
| P-006 | `docs/ROUTES.md` — old→new mapping        | All routes     | todo   | For redirect planning                  |
| P-007 | `docs/DROPPED.md` — intentional omissions | Dropped items  | todo   | With justification                     |
| P-008 | `docs/RETROSPECTIVE.md`                   | —              | todo   | §13 — cited from notes                 |

---

## Current Focus

| ID    | Title             | Status |
| ----- | ----------------- | ------ |
| D-010 | Markdown renderer | todo   |

---

## Blocked

| ID  | Title | Reason |
| --- | ----- | ------ |
| —   | —     | —      |

---

## Dropped

| ID  | Title | Reason           |
| --- | ----- | ---------------- |
| —   | —     | See `DROPPED.md` |
