# ReCodEx New Frontend — Design Decisions

**Status:** Living document
**Date:** 2026-05-11

---

## Architecture

| #       | Decision                                       | Rationale                                                                                             | Alternatives Considered                                            |
| ------- | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| DEC-001 | **Next.js 16.3 App Router**                    | Required by brief. Server Components native. App Router is the future of Next.js.                     | Next.js 15 (older), Remix, SvelteKit                               |
| DEC-002 | **TypeScript strict mode**                     | Catch errors at build time, not runtime. Brief requirement.                                           | Loose TS (faster, less safe)                                       |
| DEC-003 | **pnpm over yarn/npm**                         | Strict `node_modules` catches phantom deps. Better Docker layer caching. Brief requirement.           | yarn (legacy uses it), npm                                         |
| DEC-004 | **Tailwind CSS + shadcn/ui**                   | Modern, utility-first. shadcn/ui provides accessible primitives. Vendored into repo for full control. | CSS Modules, styled-components, Chakra UI                          |
| DEC-005 | **Server Components by default**               | Brief §6.4: `"use client"` on interactive leaves only. Reduces client bundle.                         | Client-heavy SPA (legacy approach)                                 |
| DEC-006 | **httpOnly cookie + BFF for auth**             | Brief §5: token never reaches client JS. SameSite=lax for CSRF protection.                            | localStorage (legacy — insecure), sessionStorage                   |
| DEC-007 | **Two API URLs (internal + public)**           | Brief §4: separate URLs for server-side fetches and browser-facing redirects.                         | Single URL (breaks in container)                                   |
| DEC-008 | **Next-intl for i18n**                         | App Router-native, works in Server Components. cs/en both required.                                   | react-intl (legacy), i18next                                       |
| DEC-009 | **TanStack Query for client data**             | Brief §4: only for genuinely client-driven cases. Caching, deduplication, background refetch.         | SWR, Apollo Client, raw fetch                                      |
| DEC-010 | **Server Actions for mutations**               | Brief §4: Zod-validated, revalidateTag/Path. Public HTTP endpoint with nice syntax.                   | API Routes, tRPC                                                   |
| DEC-011 | **Route Handlers for file uploads**            | Brief §6.7: Server Actions have 1MB body limit. Route Handlers stream to core-api.                    | Server Actions (insufficient), direct browser-to-API (breaks auth) |
| DEC-012 | **Shiki for code display**                     | Server-side rendering, no client JS. Better than highlight.js/prism.                                  | highlight.js (legacy), prismjs                                     |
| DEC-013 | **CodeMirror 6 for editing**                   | Modern, accessible, extensible. Client-only via `next/dynamic`.                                       | Ace Editor (legacy), Monaco                                        |
| DEC-014 | **React markdown + remark-gfm + rehype-katex** | Modern, maintained. Must test compatibility with legacy markdown-it.                                  | markdown-it (legacy), MDX                                          |

---

## IA & Routing

| #       | Decision                        | Rationale                                                                                                     | Alternatives Considered                |
| ------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| DEC-015 | **No persona route groups**     | Brief §2: one person is routinely both student and teacher. Routing follows context (group), not global role. | `(student)` / `(teacher)` route groups |
| DEC-016 | **Dashboard as landing page**   | Brief §2: "what do I owe, what am I responsible for" on one screen. No unnecessary clicks.                    | Legacy home page → dashboard redirect  |
| DEC-017 | **Group-centric navigation**    | Brief §9: navigation reflects how people work, not database shape. Groups are the primary context.            | Exercise-centric, user-centric         |
| DEC-018 | **Central breadcrumb manifest** | Brief §6.12: breadcrumbs from one mechanism, async resolvers for entity names.                                | Per-page breadcrumbs (inconsistent)    |
| DEC-019 | **Deep-linkable searchParams**  | Brief §9: filters, tabs, pagination, sort in URL. Shareable state.                                            | Local state only (lost on refresh)     |
| DEC-020 | **`/dev/kitchen-sink` route**   | Brief §10: cheaper than Storybook for internal component showcase.                                            | Storybook (heavier, separate build)    |

---

## State & Data

| #       | Decision                                                                                                                                                                                                                               | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Alternatives Considered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEC-021 | **No client-side auth state**                                                                                                                                                                                                          | Auth state lives in httpOnly cookie. Server Components read from `cookies()`. Client components never see the token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Redux auth state (legacy), Context API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-022 | **`requireSession()` on every data access**                                                                                                                                                                                            | Brief §5: authorisation boundary is the data access layer, not the page or layout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Middleware-only auth (insecure — §6.1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-023 | **Server Actions for simple mutations (not login/logout)**                                                                                                                                                                             | Form submissions (group creation, exercise editing, etc.), Zod-validated on server. **Corrected during F-016**: this row originally also listed "Login, logout" — recon-phase error, contradicted by the brief's own §5 text ("**Login**: Route Handler receives credentials...") and by `docs/BACKLOG.md`'s own F-016/F-017 titles ("Auth BFF: login Route Handler" / "...logout Route Handler"), which the recon phase apparently wrote down correctly in one document and then contradicted in this one. Login and logout are Route Handlers — see DEC-038.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | API Routes (more boilerplate)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| DEC-024 | **TanStack Query for live data**                                                                                                                                                                                                       | Evaluation progress, notifications. Polling or WebSocket-backed query.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | setInterval polling (no deduplication)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-025 | **URL state for UI preferences**                                                                                                                                                                                                       | Tab, filter, sort, page in `searchParams`. Survives refresh, shareable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | LocalStorage (not shareable), sessionStorage                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-026 | **`Dockerfile` lives in this repo's own root, not the compose repo's `services/<name>/`**                                                                                                                                              | The `services/<name>` pattern the other ReCodEx components use exists because their source is pulled by the compose repo's `pull-repos.sh` from `github.com/ReCodEx/*` into a shared build context. This repo isn't one of those — it's this project's own separate repo. A self-contained Dockerfile (standard practice for a standalone Next.js app) is simpler and doesn't force an artificial cross-repo COPY. The compose entry (F-004) will point its `build.context` at this repo's path instead of the compose repo root.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mirroring `services/<name>/` anyway (rejected — adds a COPY indirection for no benefit, since there's no upstream-pull step to share)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-027 | **`outputFileTracingRoot` + `outputFileTracingIncludes` in `next.config.ts`**                                                                                                                                                          | Next's standalone-output file tracer (NFT) under-includes `@swc/helpers` with this pnpm layout — copies `cjs/` but not `esm/`, which `next`'s own `require-hook.js` needs at runtime. Diagnosed by actually running the built image (`docker run`), reading the crash, and diffing the traced output against the full pnpm store — not guessed from a GitHub issue. `outputFileTracingRoot` alone (the fix suggested for the superficially-similar "monorepo symlink" issue) did **not** fix it; `outputFileTracingIncludes: { "/**": [".../@swc/helpers/**"] }` did. Kept both — the root is harmless/recommended regardless.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `node-linker: hoisted` in `pnpm-workspace.yaml` (rejected — defeats DEC-003's strict-`node_modules` rationale for phantom-dependency detection); manually copying the missing directory in the Dockerfile as a `RUN cp` step (rejected — `next.config.ts` is the correct layer, not a build-script patch)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| DEC-028 | **`web-next` compose service publishes its own host port directly, bypassing `proxy`**                                                                                                                                                 | F-004. Operator approved this shape as shown, unedited. Simpler for side-by-side dev against the legacy `web-app` (still on `:8080` behind `proxy` on `:80`) — no `nginx.conf.template` changes needed, no risk to the working legacy path while this app is still pre-parity. Container's internal `PORT` is `3000` (matches `.env.local`/Dockerfile default); host mapping is `WEB_NEXT_PORT=3001` — `3000` was already bound by something else on the host, discovered live at `docker compose up -d` and switched without further operator round-trip (recorded here, not asked about, since it's a pure host-port renumbering with zero behavioural difference). `API_BASE_INTERNAL` changed from equal-to-public (F-006's placeholder, see PROGRESS.md) to the real internal hostname `http://api:80/v1`, now that the container is genuinely on the `recodex` network — verified reachable via `docker exec ... getent hosts api`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Routing `web-next` through `proxy` on a subpath (e.g. `/next/`) instead (rejected for now — adds `nginx.conf.template` risk to a working legacy deployment for no benefit while this app is pre-parity; revisit once cutover is closer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| DEC-029 | **`scripts/seed.ts` builds exactly one gradeable exercise, reused via many assignments**                                                                                                                                               | F-025. Creating a fully-configured, gradeable exercise via the public API is genuinely a 7-step chain (hardware-groups, environment-configs, tests, detail, supplementary file + config with per-pipeline variable discovery, limits, reference solution) with no documented shortcut — confirmed empirically against the live instance, not assumed from the swagger spec alone (which is too generic on the `tests`/`config`/`limits` shapes to build from directly; the real shapes came from reading `repos/web-app/src/helpers/exercise/configSimple.js` and `configAdvanced.js`). Since ReCodEx allows assigning the _same_ exercise to a group multiple times (verified live — two `POST /exercise-assignments` calls with identical `exerciseId`+`groupId` both succeed with distinct IDs), the pagination-filler requirement (25 assignments) is satisfied by reusing one exercise rather than repeating the 7-step build 25 times.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | One exercise per assignment (rejected — 25x the API calls and the fragile per-exercise config/variable discovery dance, for no benefit since the brief only needs _rows_, not _distinct_ exercises)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-030 | **Reference solutions are required to exist (not to be evaluated) before an exercise can be assigned**                                                                                                                                 | Discovered live: `POST /v1/exercise-assignments` 400s with "Exercise does not have any reference solutions" until at least one `POST /v1/reference-solutions/exercise/{id}/submit` has been made — but the check is existence-only, it does not wait for or require a successful evaluation. Relevant given DEC-031 (evaluation is currently broken on this dev machine) — the seed script's own reference solution submission does not need to (and cannot, right now) reach a real pass/fail either.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DEC-031 | **Real pass/fail evaluation states cannot be produced on this dev machine right now**                                                                                                                                                  | `scripts/seed.ts`'s submissions resolve to an infrastructure `evaluation_failure` ("Isolate init error. Return value: 2"), confirmed via `docker logs recodex-worker-1` showing `Checking for cgroup support for memory ... CAUTION`. This is the **same, already-documented** limitation in the compose repo's `README.md` ("Before going to production, read this: worker needs cgroup v1" — cgroup v2-only hosts, explicitly including "macOS/Docker Desktop", cannot run the vendored isolate 1.8.1 sandbox) — re-discovered while verifying F-025 end-to-end, not a regression introduced by this work. The seed script still submits logically-correct-vs-logically-wrong solutions (so the _data_ is right), but genuine pass/fail differentiation must be re-verified on a cgroup v1 host before relying on it for evaluation-state UI work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Attempting to fix Docker Desktop's cgroup mode from within this session (rejected — host/VM-level change outside this repo's scope, already investigated and documented as a production prerequisite, not a bug to patch around)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| DEC-032 | **Theme tokens: Tailwind v4 CSS-first config + shadcn/ui's own variable names + `oklch` + `next-themes`**                                                                                                                              | F-010. Tailwind v4 (latest, `4.3.3` per `npm view`) replaced the JS `tailwind.config.js` with a CSS-first `@theme`/`@import "tailwindcss"` approach — installed `tailwindcss` + `@tailwindcss/postcss` (the official Next.js/Turbopack integration path) rather than the old `tailwind.config.ts` + `postcss-import` setup. Token _names_ (`--background`, `--foreground`, `--primary`, etc.) match shadcn/ui's own convention exactly, since DEC-004 already commits to vendoring shadcn/ui — using its expected variable names now means future `shadcn add` component output will just work against these tokens instead of needing a rename pass later. `oklch` is shadcn's current default color space (perceptually uniform, better interpolation); Lightning CSS (Tailwind v4's bundled CSS transform) auto-downgrades it to `lab()`/hex for older browsers — confirmed in the actual compiled `.next/static/chunks/*.css` output, not assumed. `next-themes` provides the `.dark` class toggle + a blocking anti-FOUC inline script (`attribute="class"`, `defaultTheme="system"`, `enableSystem`) — confirmed present and correctly configured in the rendered HTML (`curl` against a real `next start`), not just "the package is installed".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Tailwind v3 + JS config (rejected — not the current major version, brief's own "verify latest patch" philosophy applies to majors too here since v4 is a real breaking change, not a patch); rolling a custom light/dark mechanism instead of `next-themes` (rejected — reinvents a small, standard, well-tested package for no benefit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-033 | **i18n: `next-intl@4.13.7` with `app/[locale]/...`, `localePrefix: "always"`, `requestLocale` (not `next/root-params`)**                                                                                                               | F-011. `next-intl` per DEC-008; version/routing API verified against the actually-installed package (`node_modules/.pnpm/next-intl@.../dist/types/**`), not from memory — e.g. confirmed `defineRouting`'s default `localePrefix` is already `"always"`, and confirmed the plugin's default `i18n/request.ts` search path by reading its compiled `getNextConfig.js`. `localePrefix: "always"` (both `/en/...` and `/cs/...` explicitly prefixed, `/` redirects) rather than `"as-needed"`, matching the brief's "Czech and English, both complete, always" — neither locale is an implicit unprefixed default outranking the other. `getRequestConfig`'s `requestLocale` param is marked `@deprecated` in favor of `next/root-params`, but checked and **`next/root-params` isn't actually usable in the installed Next 16.3.1**: its files are listed in `next`'s `package.json` `"files"` array but don't exist in `node_modules` — so `requestLocale` was used anyway as the only currently-working option; revisit when root-params actually ships (brief §6 footgun 11 explicitly calls for verifying this rather than assuming). `defaultLocale: "en"` matches the legacy app's own fallback (`repos/web-app/src/locales/index.js`'s `getDefaultLang()` falls back to `'en'` when `navigator.language` isn't `cs`/`en`). Root route structure: `app/[locale]/layout.tsx` **is** the root layout (renders `<html>`/`<body>`) — no separate top-level `app/layout.tsx`, since `[locale]` is the only top-level segment; `app/globals.css` stays at `app/` root (locale-independent) and is imported with a relative `../globals.css` path. `proxy.ts` (not `middleware.ts` — footgun 1) wraps `next-intl/middleware`'s default export directly, confirmed valid per `node_modules/next/dist/docs/.../proxy.md`'s "default export or named `proxy`" rule. Verified end-to-end for real: `next build` (both `/en`/`/cs` statically prerendered via `generateStaticParams`, `Proxy (Middleware)` present in the route list), a live `next dev` session (`/` → 307 to `/en` with no `Accept-Language`, → `/cs` with `Accept-Language: cs`, both locale pages render with the correct `<html lang>`), and — since proxy/middleware bundling under `output: standalone` has a history of subtle breakage — a full `docker build` + `docker run` repeating the same three checks against the actual production container image, not just `next dev`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `localePrefix: "as-needed"` (rejected — would make English the implicit unprefixed default, contradicting "both complete, always"); porting all ~2200 legacy message keys now (rejected — nothing to translate yet beyond the placeholder page; real message migration happens per-screen as each S-/T-/D-series ticket lands, sourced from `repos/web-app/src/locales/{en,cs}.json`'s wording, not its flat `app.foo.bar` key scheme, which next-intl doesn't use)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-034 | **Error/not-found/forbidden conventions: standard `error.tsx`/`not-found.tsx` (not `catchError`) + `forbidden()`/`unauthorized()` behind `experimental.authInterrupts` + `global-not-found.tsx` behind `experimental.globalNotFound`** | F-012. `error.tsx`'s `retry` prop is stable since 16.3.0 and gives route-segment error boundaries the same re-fetch-and-re-render recovery `catchError` provides — confirmed via the bundled docs' own "Good to know": "You don't need to wrap `error.js` default exports with `catchError`." `catchError` (AGENTS.md footgun 8) is reserved for _component-level_ boundaries that aren't tied to a route segment (e.g. a single dashboard widget) — none exist yet, so not used here. `forbidden()`/`unauthorized()` are marked `experimental` in the bundled docs and gated behind `experimental.authInterrupts`; enabled anyway since there's no non-experimental way to get a real 403/401 status from the App Router and ReCodEx is permission-heavy throughout (groups, exercises, admin) — ADR philosophy matches DEF-001/DEF-002's "don't enable experimental features without a specific reason," and F-012 _is_ that reason. **Found live, not from docs alone:** a bogus path under a valid locale prefix (e.g. `/en/bogus`) fell through to Next's bare built-in 404 instead of the themed `app/[locale]/not-found.tsx` — the bundled `not-found.js` doc explicitly names this failure mode ("root layout... top-level dynamic segments... makes composing a consistent 404 page harder") and its fix, `global-not-found.tsx` (also experimental); added it and re-verified the same bogus-path request now renders the themed page. `app/global-error.tsx` and `app/global-not-found.tsx` both bypass the normal `[locale]` tree (Next's own docs: they render their own `<html>`/`<body>` and don't get app styles/theme/next-intl context), so both use hardcoded bilingual (en/cs together) text rather than `getTranslations` — the deliberate, narrow exception to "no hardcoded user-facing strings," since these are exactly the two places where the normal i18n machinery itself may be what failed or may never have loaded. Verified end-to-end for real via temporary throwaway test routes (`forbidden()`, `unauthorized()`, a thrown `Error`) plus a genuinely bogus URL and an invalid-locale URL, each checked for both the correct HTTP status (403/401/500/404) and the correct rendered text, in three environments: `next build`'s route manifest, a live `next dev` session, and a full `docker build` + `docker run` against the production standalone container (test routes deleted before committing; not part of the shipped code).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `catchError` for route-segment boundaries (rejected per the docs' own guidance above); leaving `forbidden`/`unauthorized` unbuilt until F-016+ auth lands (rejected — F-012 is specifically the ticket for this convention, and building it once now means every later permission check just calls the function, no boilerplate); skipping `global-not-found.tsx` (rejected after live-testing showed the common "bogus URL under a valid locale" case was hitting Next's unthemed default 404, not a rare edge case)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-035 | **F-013 route skeleton: top-level static routes only, real content deferred, shared `PlaceholderPage`**                                                                                                                                | `(anon)`/`(app)` route groups under `app/[locale]/...` (organizational only — route groups never add a URL segment, confirmed in `next build`'s route list: `/[locale]/dashboard`, not `/[locale]/(app)/dashboard`) split by auth requirement, not persona (DEC-015 still holds — no student/teacher split). Scoped to the **top-level static paths** named directly in `docs/IA.md`'s sitemap (16 routes: 7 anon, 9 app) — deliberately excludes every dynamic segment (`/groups/[groupId]`, `/exercises/[id]/edit-config`, etc.), since those belong to the S-/T-/A-/AD- ticket that actually builds real data-fetching against them; a stub for a route with no real ID behind it proves nothing structural that the static routes don't already prove. Each stub renders a shared `components/placeholder-page.tsx` with its own translated title (sourced from the legacy app's actual sidebar/page-title strings in `repos/web-app/src/locales/{en,cs}.json` — e.g. confirmed `app.sidebar.menu.exercises` is "Úlohy" in Czech, not the more literal "Cvičení" a guess would have produced) and one shared "not built yet" message. Both route-group layouts are deliberate passthroughs (`<div>{children}</div>`) with a comment pointing at the ticket that replaces them (D-series for chrome, F-015 for the actual `(app)` access check) — not pre-built speculatively. Stub pages render dynamically (no `generateStaticParams`/`setRequestLocale`), unlike the `[locale]` root page — not worth the extra per-page boilerplate for placeholder content about to be replaced. Verified all three ways again: `next build`'s route list, a live `next dev` session (status + translated content for a sample of routes in both locales), and a full `docker build` + `docker run`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Building every nested dynamic route now too (rejected — no real ID/data to render against yet, and duplicates work the owning phase ticket will do properly); per-page bespoke placeholder text instead of a shared component (rejected — pure duplication, the "not built yet" copy is identical everywhere); guessing Czech titles instead of sourcing them from the legacy app (rejected — `app.sidebar.menu.exercises` is real, checkable ground truth, not a translation call to make up)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-036 | **`proxy.ts` auth redirect: presence-only cookie check, three-way pathname split, composed with next-intl by short-circuiting on its redirects**                                                                                       | F-014. Brief §5 is explicit that `proxy.ts` is _not_ the security boundary ("Four of the thirteen May 2026 advisories were middleware/proxy bypasses") — this only checks `request.cookies.has(SESSION_COOKIE_NAME)`, never validates the token; the real check is `requireSession()` in the data layer, landing in F-015. New `lib/auth/session-cookie.ts` exports just the cookie _name_ (derived from `SESSION_COOKIE_PREFIX`) so F-016 (login handler, which actually sets the cookie) can't drift from what this file checks. Pathnames split three ways: `AUTH_ONLY_PATHNAMES` (login, register — redirected to `/dashboard` if a session cookie is already present, since showing that form to a signed-in visitor is pointless), `PUBLIC_PATHNAMES` (forgot-password [+ change], email-verification, accept-invitation, faq, and `/` — reachable regardless of auth state; a signed-in user resetting a password or reading the FAQ is normal, not a bug), and everything else (every `(app)` route — redirected to `/login?from=<original path>` if no cookie). This list is maintained by hand against the `(anon)`/`(app)` folders from F-013, since route groups never appear in the URL and can't be introspected at runtime. **Composition with next-intl's own middleware**: run `intlMiddleware(request)` first; if its response is itself a redirect (locale detection sending bare `/dashboard` to `/en/dashboard`), return that immediately rather than layering auth logic on an in-flight locale redirect — the browser's follow-up request hits `proxy.ts` again with a resolved locale, and the auth check applies cleanly then. Verified this exact chain live: `curl` on bare `/dashboard` (no locale, no cookie) returns a 307 to `/en/dashboard`; following that redirect lands on `/en/login?from=%2Fen%2Fdashboard`, not a broken intermediate state. All six presence/pathname combinations (app-route × cookie-present/absent, login × cookie-present/absent, public-page × cookie-present/absent) and the chained-redirect case verified with real requests against both `next dev` and a full `docker build`+`run` — proxy/middleware bundling under `output: standalone` already had a real history of subtle breakage in this project (F-011), so `next dev` alone wasn't trusted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Validating the token in `proxy.ts` (rejected outright — brief §5's explicit warning, and the whole reason `requireSession()` exists as the single boundary in F-015); redirecting `/` based on auth state too (rejected for now — `/` isn't yet cleanly owned by either route group, see DEC-016's note that dashboard-as-landing-page was chosen specifically _instead of_ a home-page-redirects-to-dashboard pattern; revisit once `/` itself is assigned real behavior); redirecting forgot-password/email-verification/accept-invitation/faq away from signed-in users (rejected — legitimate to visit while signed in, unlike login/register)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-037 | **`requireSession()`: decode-and-check-expiry only, no signature verification, redirects with no `?from=`, never called from a layout**                                                                                                | F-015, the actual authorisation boundary (brief §5). Decodes the session cookie's JWT payload (base64url, `Buffer.from(part, "base64url")`, no library) purely to read `sub`/`exp` — grounded in a **real token from a live login** (`docker` stack still running from F-025), not an assumed shape: confirmed `sub` is the user UUID and `exp`/`iat`/`nbf` are Unix-seconds claims, plus an `effrole` (null on a normal login — presumably populated for F-021's restricted-token case) and a `scopes` array (`["master","refresh"]` on a fresh login — useful groundwork for F-018's refresh logic later). Deliberately does **not** verify the JWT signature: this cookie only ever holds a token _we_ set verbatim from core-api's own login response, so re-verifying our own signature proves nothing — the real validation happens when core-api itself rejects a bad Authorization header on the next actual API call. Redirects to a plain `/login` (no `?from=`) on failure, unlike `proxy.ts`'s redirect — this is the defense-in-depth/direct-invocation path (a bypassed proxy check, or a Server Action running after the page's cookie has since expired), not the common case, and not worth threading the current path through every future caller for. **Not wired into `(app)/layout.tsx`** — brief §5 is explicit: "Not the caller. Not the page. Not the layout. Itself," meaning every individual data-touching function must call this itself once real data-fetching (F-022) exists, rather than relying on one layout-level choke point that could itself become a bypassable middleware-like boundary. Verified live via a temporary test route (deleted before commit, same pattern as F-012): no cookie (caught upstream by `proxy.ts`), a real valid JWT from an actual login (correct `{token, userId}` returned), a JWT-shaped-but-expired token (redirected), and a garbage cookie value (redirected) — all four behave correctly, and the expired/malformed cases specifically prove this adds real checking beyond `proxy.ts`'s shallow presence-only test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Verifying the JWT signature locally too (rejected — no shared secret to verify against belongs in this app; core-api is the signing authority and the sole source of truth for whether a token is genuinely valid); preserving `?from=` in `requireSession()`'s redirect (rejected for now — adds complexity for what should be the rare path once `proxy.ts` is doing its job; revisit if it turns out Server Actions hit this often enough to matter); gating `(app)/layout.tsx` with this function (rejected per the brief's explicit "not the layout" — would recreate exactly the single-choke-point risk §5 warns about)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| DEC-038 | **Login is a Route Handler (corrects DEC-023), `sessionCookieOptions()` derives `secure` from `API_BASE_PUBLIC`'s scheme, cookie `maxAge` sized from the JWT's real `exp`**                                                            | F-016. `app/api/auth/login/route.ts` — brief §5's own words: "Login: Route Handler receives credentials → calls core-api → stores the token in an httpOnly, sameSite=lax cookie." DEC-023 originally listed "Login, logout" under Server Actions, contradicting this and contradicting `docs/BACKLOG.md`'s own F-016/F-017 titles — corrected in place rather than left standing. New `sessionCookieOptions(maxAgeSeconds?)` in `lib/auth/session-cookie.ts`: `httpOnly: true, sameSite: "lax"`, and `secure` derived from `new URL(API_BASE_PUBLIC).protocol === "https:"` — **not** `NODE_ENV === "production"`, since this deployment can (and currently does, see the compose repo's `README.md`) run "in production" over plain HTTP with no TLS certificate yet; `secure: true` there would make the browser silently drop the cookie, exactly the "login looks like it worked but never signs you in" failure brief §5 warns about by name. `API_BASE_PUBLIC` was chosen over reading the live request's `X-Forwarded-Proto` header (confirmed nginx sets it, the compose repo's `services/proxy/nginx.conf.template`) specifically so the helper's signature stays request-free — brief §5 calls for "one helper... used everywhere," and threading a request object into every call site would work against that. Route Handler presents a clean `{email, password}` contract to the frontend and maps `email` → core-api's own `username` field internally (confirmed live: ReCodEx has no separate "username" concept, it's just core-api's internal field name for the email). Input validated with `z.email()` (not the deprecated `z.string().email()` chain — confirmed against the installed zod 4.4.3's own type defs, a real API change this session would have gotten wrong from v3-era memory). Extracted the JWT-payload decoder from F-015's `require-session.ts` into a shared `lib/auth/jwt.ts` (`decodeJwtPayload`) once a second real consumer (sizing the cookie's `maxAge` against the token's actual `exp`, rather than a guessed constant) appeared, rather than duplicating the parsing logic. Verified for real, repeatedly, not just "the build succeeds": correct credentials (200, `Set-Cookie` with the exact right `Max-Age` and no `Secure` flag on this plain-HTTP deployment, confirmed byte-for-byte against `curl -i`'s raw header), wrong password (401, core-api's own message passed through), malformed input (400, Zod), and the **full round trip** — login, save the cookie, use it against `proxy.ts` (F-014), use it against `requireSession()` (F-015), confirm `/login` itself now redirects away — first in `next dev`, then again against the actual production compose container talking to `api` over the internal Docker network (`http://api:80/v1`), not the dev-mode public-URL fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Server Action for login (rejected — contradicts the brief's explicit, unambiguous §5 wording, and DEC-023 was simply wrong on this point); `NODE_ENV`-based `secure` flag (rejected — provably wrong for this specific deployment's current state, see above); a fixed/guessed cookie `maxAge` instead of deriving it from the token's own `exp` (rejected — would let the cookie outlive the token, or expire it early, for no reason when the real value is one JWT decode away); passing the request object into `sessionCookieOptions()` to read `X-Forwarded-Proto` per-call (rejected — works, but fights the brief's "one helper, used everywhere" framing by making every call site thread a request through)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| DEC-039 | **`request.url` is unreliable in a `output: standalone` Route Handler — use the `Host` header instead**                                                                                                                                | F-017. Discovered live, not in any doc: `app/api/auth/logout/route.ts` originally built its redirect target with `new URL("/login", request.url)` (the same pattern used everywhere else in this codebase, e.g. `redirect()` calls). Worked fine in `next dev`, but in the actual `output: standalone` Docker container, the resulting `Location` header pointed at `http://0.0.0.0:3000/login` — the container's own internal bind address (`HOSTNAME`/`PORT` from the Dockerfile) — instead of the address the browser actually used (confirmed with a temporary debug endpoint, not committed, echoing both `request.url` and `request.headers.get("host")`: the `Host` header was correct — `localhost:3001` — while `request.url` was not). This is specific to plain `Request.url` in a standalone-served Route Handler; `proxy.ts`'s `request.nextUrl` does **not** have this problem (re-confirmed against F-014's already-verified behavior). A relative `Location` header was tried as a simpler fix and also failed — confirmed live, `NextResponse.redirect()` throws `Error: URL is malformed "/login". Please use only absolute URLs`, despite its parameter being typed as accepting a plain `string`. The working fix: build the absolute URL from `request.headers.get("host")` (confirmed correct) plus the protocol from `API_BASE_PUBLIC` (same derivation as DEC-038's `sessionCookieOptions()`). **Any future code that builds an absolute URL from `request.url` inside a Route Handler should be treated as suspect and re-verified against a real `output: standalone` build, not just `next dev`** — this is exactly the kind of divergence that only shows up under the production build config, matching the pattern already seen twice before in this project (F-011's `@swc/helpers` tracing gap, F-012's `global-not-found` gap).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Reconstructing the URL from `request.url` with a manual host substitution (rejected — more code than just reading `Host` directly, and still depends on the broken value for the protocol/path parts); using a relative redirect string (rejected — confirmed to throw at runtime despite the type signature suggesting it would work)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| DEC-040 | **Token refresh: proactive, 24h-before-expiry threshold, in-memory per-token de-duplication in `proxy.ts`**                                                                                                                            | F-018. `lib/auth/refresh-session.ts`'s `maybeRefreshSession()`, called from `proxy.ts` (brief §5: "Token refresh and expiry handling happen in `proxy.ts` or Route Handlers only") and applied to whichever response `proxy.ts` ends up returning, not just the pass-through case. Threshold is a flat 24h before the token's own `exp` (tokens last 7 days by default, confirmed live) -- simple, ample buffer. De-duplication keyed by the token string itself, not `userId`: a `Map<string, Promise<...>>` at module scope, correct within one running Node.js process (this deployment's current single-instance shape, ASS-004), not across horizontally-scaled replicas -- noted as a real limitation, not silently assumed away. **Two things discovered only by testing live against core-api, not from any doc**: (1) refreshing does not invalidate the token used to request it -- both old and new tokens kept working afterward, so the actual failure mode is "waste a redundant call," not the brief's literal "invalidate each other"; (2) core-api's JWTs are second-granularity deterministic (HMAC-SHA256 over a payload whose only varying claim is `iat`) -- two refresh calls within the same wall-clock second produce a byte-identical token, which would make a broken de-duplication test (comparing resulting token strings) look correct by accident. Verified properly instead by counting actual upstream calls (temporary logging, not committed): 6 concurrent requests sharing one soon-to-expire cookie produced exactly one upstream `POST /login/refresh` call, all 6 resolving in ~150ms. Also verified the negative case with the real 24h threshold: a freshly-issued 7-day token triggers no refresh at all.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fraction-of-lifetime threshold instead of a flat 24h (rejected -- needless complexity for a fixed, known token lifetime); de-duplicating by `userId` instead of the token string (rejected -- token-as-key is simpler and self-invalidating); verifying de-duplication by comparing final token values (rejected after discovering the same-second-determinism issue -- would have been a false-positive-prone test)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-041 | **External-auth (CAS-and-similar) callback: generic `[authenticatorName]` Route Handler, `token` query param, no ticket-exchange step**                                                                                                | F-019. `app/api/auth/external/[authenticatorName]/callback/route.ts`. Brief §5 describes this only loosely ("exchanges the ticket with core-api"); the legacy app's own two implementations disagree with each other -- `repos/web-app/src/helpers/cas.js` is a CUNI-specific popup + client-side CAS `serviceValidate` flow using a `ticket` param, hardcoded to `idp.cuni.cz`, while the page actually wired into the login flow, `repos/web-app/src/pages/LoginExternFinalization/LoginExternFinalization.js`, reads a `token` param and does nothing CAS-specific at all. Resolved by reading core-api's own PHP source (`repos/api/app/helpers/ExternalLogin/ExternalServiceAuthenticator.php`'s `decodeToken()`), not by guessing between the two: core-api never talks to CAS or any external provider itself -- each `authenticatorName` has a pre-shared `jwtSecret` configured server-side, and core-api's whole job is to verify a signature on whatever token this route hands it. So `LoginExternFinalization.js`'s simpler `token`-relay model is the real, general mechanism; `cas.js` describes one specific university's own external bridge infrastructure (outside this project's repos), not something core-api implements. `[authenticatorName]` is a dynamic segment (not hardcoded to `cas`), matching core-api's own generic `/login/{authenticatorName}` and the legacy config's `EXTERNAL_AUTH_SERVICE_ID`. On a `token` present, POSTs it to `${API_BASE_INTERNAL}/login/{authenticatorName}` and reuses F-016's `establishSession()` on success (extracted from login's inline logic into `lib/auth/session-cookie.ts` for this second call site); any failure (missing `token`, non-2xx from core-api, or an undecodable token) redirects to `/login?externalAuthError=1` rather than throwing. Redirects go through a new `lib/http/absolute-url.ts` (`buildAbsoluteUrl()`, also extracted from F-017's logout for this second call site) rather than `new URL(path, request.url)`, per DEC-039. **Verified live in both `next dev` and a rebuilt `output: standalone` Docker container**: unknown `authenticatorName` correctly 400s from core-api and redirects to the error page rather than throwing; missing `token` does the same; login/logout still work after the `establishSession`/`buildAbsoluteUrl` refactor (regression check). Also specifically confirmed in the Docker build (temporary logging, not committed, exactly because this is where DEC-039's bug was originally found) that although `request.url`'s origin is wrong under `standalone` (internal bind address, e.g. `0.0.0.0:3000`), its path+query portion is intact -- `new URL(request.url).searchParams` correctly read a real query param end to end. **Not, and can't yet be, verified**: the actual success path against a real external identity provider -- this deployment has no `EXTERNAL_AUTH_*` configured at all (`docs/QUESTIONS.md` Q-004), so there's no way to obtain a validly-signed external token to test against. Corrected a stale recon-phase assumption in `docs/IA.md` (line ~310), which had guessed a CUNI-specific `/login/extern-finalization/:service` → `/api/auth/cas/callback` shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Hardcoding a `/api/auth/cas/callback` route per `cas.js`'s CUNI-specific model (rejected -- reading core-api's own source showed this isn't what core-api implements; a hardcoded single-provider route would also contradict the generic `authenticatorName` config the legacy app and core-api both actually use); reproducing `cas.js`'s client-side popup + `serviceValidate` mechanic (rejected -- that's a university-specific external bridge's own behavior, not something this app or core-api needs to implement); leaving the route unbuilt since it's unverifiable end-to-end here (rejected -- `docs/QUESTIONS.md` Q-004 already called for building it regardless, for parity, same reasoning as local registration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| DEC-042 | **User takeover: `POST /api/auth/takeover/[userId]`, authorization delegated entirely to core-api**                                                                                                                                    | F-020. `app/api/auth/takeover/[userId]/route.ts`. Mirrors core-api's own `POST /v1/login/takeover/{userId}` 1:1 (dynamic segment, not a body field), confirmed against `docs/swagger.yaml` and `LoginPresenter::actionTakeOver()`/`checkTakeOver()`. Response shape is identical to login and the external-auth callback (`{payload: {accessToken, user}}`), so this route reuses the same `establishSession()`. This route enforces only one thing itself -- that some caller session exists at all (401 if not) -- and forwards it as `Authorization: Bearer <token>`; whether _that_ caller may take over _this_ target user is entirely core-api's call, not duplicated here. Confirmed directly in `repos/api/app/config/permissions.neon`, not assumed from the BACKLOG.md note: `takeOver` is allowed only for `role: superadmin`, with an explicit `allow: false` safety net underneath for everyone else, specifically to prevent a wildcard rule from accidentally granting it. A non-superadmin's attempt surfaces as a JSON 403 (not a redirect to `/login`, unlike the no-session-at-all case): the caller is legitimately logged in, just not allowed to do this one thing, and bouncing them to `/login` again would be a dead end, not a fix. **Verified live in both `next dev` and a rebuilt Docker `standalone` container** against the real seeded accounts (`docs/SEED_ACCOUNTS.md`): superadmin (`admin@admin.com`) taking over a plain student correctly swaps the session cookie's `sub` to the target user; the same student attempting to take over the superadmin correctly gets a 403; no session cookie gets a 401; a malformed (non-UUID) `userId` gets a 400 from this route's own `z.uuid()` check without ever reaching core-api; a well-formed but nonexistent `userId` gets a 404 forwarded from core-api.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Re-deriving/caching an `isSuperadmin` check in the BFF from the JWT's `effrole` claim and gating the route on it before ever calling core-api (rejected -- duplicates an authorization decision core-api already makes authoritatively via `canTakeOver()`/`permissions.neon`, and DEC-022/DEC-037's own principle is that core-api is the one authority that matters, not a second copy of its logic that could drift out of sync); using `requireSession()` for the missing-session case instead of a direct cookie read (rejected -- its `redirect("/login")` behavior is right for page/data-access guards but wrong for this JSON-API-style POST route, which callers are expected to `fetch()` and read a JSON body from, not follow a redirect out of); accepting `userId` as a request body field instead of a path segment (rejected -- core-api's own endpoint is path-based, and mirroring that 1:1 is simpler, same reasoning as F-019's `[authenticatorName]`)                                                                                                                                                                                                                                                                                                        |
| DEC-043 | **Restricted (application) token generation: raw token returned to the client by design, deliberate exception to DEC-021**                                                                                                             | F-021. `app/api/auth/restricted-token/route.ts`. `POST /v1/login/issue-restricted-token`, confirmed against `docs/swagger.yaml` and `LoginPresenter::actionIssueRestrictedToken()`. Legacy app research (`GenerateTokenForm.js`, `EditUser.js`) confirmed this endpoint has two different consumers with genuinely different semantics: a self-service "Generate Application Token" form (displays the raw token for the user to copy into external scripts/tools, doesn't touch the current session) and `restrictEffectiveRole()` (a sidebar "view as role X" toggle that dispatches a `LOGIN` action, replacing the current session with the narrower one). Scoped this ticket to only the former -- the ticket title and BACKLOG.md note describe the raw BFF primitive, and no doc anywhere in this repo had already committed to building the effective-role-switch UX, so building it now would be undecided scope creep, not what this Foundation ticket asked for. Left as a future ticket (would reuse this same route's response plus its own explicit `establishSession()` call) if the Dashboard/sidebar phase decides to build it. **Deliberately returns the raw generated token in the JSON response body** -- the one intentional exception to DEC-021 ("client components never see the token") in this whole module: that decision is about the BFF's own httpOnly session cookie, not about a token the user explicitly requested to take _out_ of this app, same idea as a GitHub/GitLab personal access token generator. Never calls `establishSession()` and never touches the session cookie -- confirmed live that generating a token leaves the caller's own session cookie byte-for-byte unchanged. `scopes` is validated only as "a non-empty array of strings," not against a hardcoded copy of core-api's `TokenScope` enum -- core-api's own `validateScopeRoles()` already authoritatively rejects forbidden scopes (`change-password`, `email-verification`) and unknown `effectiveRole` values; mirroring that list here would just be one more thing to keep in sync, same reasoning as DEC-042's authorization delegation. **Verified live in both `next dev` and a rebuilt Docker `standalone` container**: a generated `read-all`-scoped token decodes to exactly the requested scopes (not `master`/`refresh`); the caller's own session cookie is unchanged after generating one; no session gets 401; an empty `scopes` array gets 400 from this route's own Zod check; `change-password` gets core-api's 403 with its own message forwarded verbatim; an unknown `effectiveRole` gets core-api's 400; `master` scope with an expiration beyond core-api's 1-week cap gets its 403, also forwarded verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Also building the effective-role-switch UX (session-replacing `restrictEffectiveRole()` equivalent) in this same ticket (rejected -- undecided UI/UX scope not asked for by this Foundation-phase ticket; better designed alongside the sidebar/dashboard work that will actually consume it); hardcoding a `TokenScope`-mirroring enum for `scopes` validation (rejected -- core-api's own validation is authoritative and already returns a clear error, a local copy could silently drift from the PHP source over time); withholding the raw token from the response and instead only ever setting it as the session cookie (rejected -- defeats the feature's actual purpose, which is explicitly to hand the user a token to use _outside_ this app)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| DEC-044 | **Typed `server-only` API client: `openapi-typescript` for path/request typing, hand-written response types, no caching**                                                                                                              | F-022. `lib/api/client.ts`, generated types at `lib/api/core-api.generated.ts` (`pnpm generate:api-types`, vendored spec at `openapi/core-api.yaml`, a copied-in snapshot of `repos/api/docs/swagger.yaml` -- CI only checks out this repo, so codegen input has to live here, not be reached cross-repo). Brief §4's own table: "API types: Generate from OpenAPI if available (`openapi-typescript`); otherwise hand-written types." Chose `openapi-typescript` alone over the companion `openapi-fetch` runtime library after actually inspecting the generated output: core-api's `swagger.yaml` has **no response schemas anywhere** -- confirmed directly, all ~250 response definitions are literally `description: 'Placeholder response'`, no `content`/`schema`, no `components.schemas` section in the 7409-line file at all. Request bodies and path params generate real, useful shapes (confirmed for several endpoints, e.g. `issue-restricted-token`'s `scopes: unknown[]` matches F-021's own hand-written Zod schema exactly) but response payloads always resolve to `never` -- so a caller must supply the actual response type `T` by hand regardless, same as this repo's auth routes have been doing all along (`{payload: {accessToken: string}}` inline interfaces). Given that, `openapi-fetch`'s main value proposition (fully inferred responses) doesn't apply here, and adding it just for request-side inference wasn't worth a second dependency with the same TS peer-range mismatch (`^5.x` vs. this repo's pinned `6.0.3` -- installs fine, `pnpm peers check` warns, verified `openapi-typescript`'s codegen itself runs and produces correct output despite the warning). `lib/api/client.ts`'s `path` parameter is typed as `keyof paths` -- a genuine compile-time guarantee the endpoint exists and is spelled correctly -- while `pathParams`/`query`/`body` stay as plain `Record`/`unknown`, and callers who want a specific endpoint's real generated request-body shape can still import `operations` directly. Calls `requireSession()` itself on every request (DEC-022/037's boundary, brief §5's own words: "Server-side reads: Server Components call a `server-only` API client that pulls the token from `cookies()`"), sets `cache: "no-store"` unconditionally on every fetch and does not wrap anything in React's `cache()` ("no user caching" per the BACKLOG.md note, and brief rule 6.3: "Never cache user-scoped data... a cross-user cache leak here is a security incident"). Unwraps core-api's fixed response envelope (`{success, error: {message, code, parameters}, payload}`, confirmed directly against `ApiErrorPresenter::sendErrorResponse()`) into a typed `ApiError` class -- `code` is the same short, stable string (`"403-002"`, not the PHP constant name) the legacy app's own `apiErrorMessages.js` keys a localised message table on, confirmed by reading `FrontendErrorMappings.php`'s constant _values_, not just their names -- intended as the future hook for a next-intl-backed equivalent table, not built by this ticket. **Bug found and fixed live, not caught by typechecking**: `API_BASE_INTERNAL`/`API_BASE_PUBLIC` already include the `/v1` prefix (every existing auth route relies on this, e.g. `${apiBase}/login`), but every generated `paths` key is _also_ absolute from core-api's root starting with `/v1` (that's how `swagger.yaml` itself defines them) -- naive concatenation produced a `/v1/v1/...` URL that 404s. Fixed by stripping the leading `/v1` from the resolved path in the client, documented inline, rather than changing what `API_BASE_INTERNAL` means (would have touched every already-verified F-016 through F-021 route for no benefit). **Verified live in both `next dev` and a rebuilt Docker `standalone` container**, via a temporary debug route (not committed): a real `GET /v1/users/{id}` call for the caller's own id returns the correctly-typed payload; a syntactically valid but nonexistent id correctly throws `ApiError` with core-api's real `httpStatus: 404`, `code: "404-000"`, and message, not a generic failure. | `openapi-fetch` for a fully generated, single-call client (rejected after inspecting actual codegen output -- its main benefit, typed responses, doesn't exist for this API since `swagger.yaml` has no response schemas; would have added a dependency and its own TS peer-version friction for request-side typing alone, which a much simpler hand-written client already provides via `keyof paths`); fully generic per-method type inference for `pathParams`/`query`/`body` from the generated `operations` type (rejected as complexity/effort not justified given the response side is unavoidably hand-typed anyway -- callers can still reach into `operations` directly for a specific endpoint's real request shape when it matters); changing `API_BASE_INTERNAL`/`API_BASE_PUBLIC` to exclude `/v1` instead of stripping it from generated paths (rejected -- would require touching every existing, already-verified auth route for a purely cosmetic difference); regenerating types from a live core-api instance in CI instead of a vendored spec (rejected -- CI has no network access to a running core-api and no cross-repo access to the compose repo; a vendored, manually-refreshed copy is the only reproducible option for this repo's actual CI setup) |
| DEC-045 | **Playwright smoke harness: API-driven login (not a form), `next start`, capped worker concurrency, not wired into CI**                                                                                                                | F-023. `playwright.config.ts`, `e2e/smoke.spec.ts`, `e2e/helpers/{accounts,auth,base-url}.ts`. Skipped `@next/playwright` (brief §4's table mentions it for its `instant()` helper) after checking its own published README: it requires Cache Components, which this repo has deliberately left off (DEF-001's `cacheComponents: false` deferral) -- using it would mean either enabling an experimental caching feature this project has explicit, documented reasons to avoid, or installing a helper with a hard requirement it can't meet. Used plain `@playwright/test` instead. Logs in via the real Auth BFF Route Handler (`app/api/auth/login/route.ts`, F-016), not a login _form_ -- there isn't one yet (`/login` is still F-013's `PlaceholderPage`). Runs against `next build` + `next start`, not `next dev`: dev mode's extra warnings/overlay aren't representative of what ships, and DEC-039's `request.url` bug only reproduced in a production `standalone` build -- confirmed `next start` still loads `.env.local` (`node_modules/next/dist/docs/.../environment-variables.md`: skipped only when `NODE_ENV=test`). Chromium only, ~10 planned core journeys deferred to later tickets per brief §8 -- this ticket is explicitly the _skeleton_ (login + visit every route implemented so far + fail on errors), not the full suite. **Not wired into `.github/workflows/ci.yml`**: these tests need a real, reachable core-api (brief: "against the real local API"), which GitHub Actions' runner doesn't have and this repo has no way to stand up on its own -- core-api/mysql/etc. live in the separate `ReCOdex` compose repo, unreachable from this repo's CI. Meant to be run locally (`pnpm test:e2e`) against a developer's own `docker compose up`'d stack. **Three real bugs found and fixed while building this, none hypothetical**: (1) `app/api/_debug/...`-style leading-underscore route directories are Next's private-folder convention and are silently excluded from routing (a first version of an unrelated debug route 404'd for this reason during F-022's own verification, same lesson applies here); (2) `test.use({storageState: path})` declared at `describe` scope silently becomes the default for _any_ context Playwright creates within that scope -- reproduced with both `browser.newContext()` and `playwright.request.newContext()` inside `beforeAll`, both threw `ENOENT` reading a file that exact call was supposed to create. Fixed by moving the login step to a plain `fetch()` (no Playwright context at all) and injecting the resulting cookie per-test via `context.addCookies()` instead of file-based `storageState`; (3) the default CPU-core-count worker total (7, on the machine this was built on) sent enough concurrent bcrypt-hashing logins to the local dev `docker compose` stack's PHP-FPM pool to make some of them genuinely time out (`ConnectTimeoutError` to `recodex.local:80`, confirmed in the Next.js server's own logs, not a code bug) -- fixed with `workers: 2`, verified stable across repeated full runs afterward. Also found and fixed a Vitest/Playwright collision: Vitest's own default include glob picked up `e2e/smoke.spec.ts` and tried to run it as a unit test, failing immediately -- excluded `e2e/**` in `vitest.config.ts`. **Verified live**: 48/48 tests pass, stably, across repeated runs -- 8 anonymous public routes plus 4 seeded personas (`docs/SEED_ACCOUNTS.md`) each visiting all 10 protected `(app)` routes, with a real session cookie, real console-error/page-error/failed-request watching, and a screenshot per route in the already-gitignored `screenshots/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `@next/playwright`'s `instant()` helper (rejected -- hard-requires Cache Components, which this repo deliberately has off); driving a real login form with Playwright's UI interactions (rejected -- no form exists yet; the API-driven approach is also simpler and won't need to change until a form ticket lands, at which point only `helpers/auth.ts` needs updating, not every test); `test.use({storageState})` with a `beforeAll`-written file (rejected after two different Playwright APIs both threw `ENOENT` from the same describe-scope option inheritance -- `context.addCookies()` from a plain `fetch()`-obtained cookie is both simpler and immune to it); leaving `workers` at its default (rejected -- reproducibly flaky against a real local backend, not a one-off); wiring `test:e2e` into CI as-is (rejected -- would be red on every run for an environment reason, not a code reason, until this repo's CI gains a way to stand up core-api, which is out of scope for this ticket).                                                                                                                                                                                                                                                                    |
| DEC-046 | **Token-leakage security test: real browser-set cookie inspection, cookie-diffing, and both HTML and JS-bundle scanning**                                                                                                              | F-024. `e2e/security.spec.ts`, the brief's §8 "one security test, non-negotiable": assert the auth cookie is httpOnly and the raw token appears nowhere in served HTML or client bundles. Deliberately does **not** reuse F-023's `loginAndGetCookie()` + `context.addCookies()` shortcut: that path manually reconstructs a cookie for rendering convenience (F-023 doesn't care about attribute fidelity, only presence), which would make this test check a cookie _this test itself_ labeled `httpOnly: true` -- a tautology, not a verification. Instead uses `page.request.post()` directly so Playwright parses the _real_ `Set-Cookie` response header from `/api/auth/login`, preserving whatever flags the server actually set. Finds the session cookie by diffing `context.cookies()` before/after login rather than assuming its name (configurable via `SESSION_COOKIE_PREFIX`) or shape -- robust to either changing without silently checking nothing. Second assertion goes further than the flag alone: reads `document.cookie` from inside the page's own JS after navigating, confirming the practical consequence (browser JS genuinely cannot read it), not just the attribute Playwright's cookie API reports. Third test scans both the full rendered HTML (`page.content()`) of every protected route and every served JavaScript response body (captured via a `page.on("response")` listener filtering on `content-type: ...javascript`) for the literal token string. **Verified the test can actually fail, not just pass by construction -- both assertions, deliberately, before trusting either**: temporarily flipped the HTML check to assert against `"html"` (guaranteed present) and confirmed a real failure with the full captured page source in the error output; temporarily flipped the httpOnly check to assert `false` and confirmed a real failure reporting `Received: true`. Both reverted immediately after confirming failure, real assertions restored, full suite re-run clean. Route lists (`PUBLIC_ROUTES`/`APP_ROUTES`) extracted from `smoke.spec.ts` into a new shared `e2e/helpers/routes.ts` once this ticket gave them a second consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Reusing F-023's `context.addCookies()`-based login for this test too (rejected -- would check a cookie whose `httpOnly` flag this test itself set, not what the server actually sends; a tautological pass that verifies nothing); hardcoding the session cookie's name instead of diffing (rejected -- `SESSION_COOKIE_PREFIX` is deployment-configurable, and a hardcoded name could silently stop matching without failing loud); only checking the `httpOnly` flag via Playwright's cookie API without the `document.cookie` follow-up (rejected -- cheap to add, and demonstrates the actual guarantee the flag is supposed to provide, not just that Playwright parsed it correctly); trusting both assertions to be correct without deliberately forcing a failure first (rejected -- a security test that has never been observed to fail is exactly the kind of test that could be silently broken and no one would notice; the brief calls this test "non-negotiable", which argues for extra rigor here specifically, not less).                                                                                                                                                                                                                                        |
| DEC-047 | **`PageShell`: plain Server Component, `breadcrumbs` required but unresolved, existing token/Link conventions reused as-is**                                                                                                           | D-001. `components/page-shell.tsx`, per `docs/IA.md` §3.3's own `PageShellProps` interface (`title`, `subtitle?`, `breadcrumbs: BreadcrumbItem[]`, `actions?`, `tabs?`, `children`), satisfying brief §9 ("No page builds its own header") and footgun #12 ("Breadcrumbs come from one mechanism"). A plain Server Component, not `"use client"`: nothing in it is interactive on its own -- `actions`/`tabs` are pre-built `ReactNode`s the caller supplies, already client components if they need interactivity, matching brief rule 4. `breadcrumbs` is a **required** prop but this component does not resolve it -- D-002 (central route manifest, async entity-name resolvers) owns turning a route into `BreadcrumbItem[]`; `PageShell` only renders whatever list it's handed, so it stays a pure presentational component with no data dependency, and D-002 can land independently without touching this file. Reused the existing shadcn-style CSS tokens (`text-foreground`/`text-muted-foreground`/`border-border`, already established in `app/globals.css` from earlier tickets) and `@/i18n/navigation`'s `Link` (not `next/link`, required for automatic locale-prefixing per next-intl's own convention already used elsewhere in this repo) -- no new dependencies, no new token system. Wired into `components/placeholder-page.tsx` (F-013's stub-page shell, used by all 18 currently-real routes) with a single, unlinked breadcrumb crumb (just the page's own title, no parent chain) as an immediate, low-risk way to verify `PageShell` renders correctly across the entire live app today, rather than waiting for the S-/T-/A-/AD- tickets that will eventually replace each stub page individually -- real breadcrumb chains are explicitly D-002's job, not faked here ahead of that ticket. **Verified live** (`next dev` and a rebuilt Docker `standalone` container): the full 50-test E2E suite (F-023/F-024) passes unchanged with `PageShell` now in the render path of every route, confirming no console errors, hydration mismatches, or unexpected 4xx/5xx were introduced; a temporary debug route (not committed) exercised the full prop surface `PlaceholderPage` doesn't touch -- `subtitle`, `actions`, `tabs`, and a linked (non-current) breadcrumb crumb -- confirming each renders and that the linked crumb produces a correctly locale-prefixed `href` (`/en/groups`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Having `PageShell` itself resolve breadcrumbs from the current route (rejected -- couples a presentational component to data-fetching/route-parsing logic that D-002 is separately, and more sensibly, scoped to own); a client component to allow, e.g., a collapsible mobile breadcrumb (rejected -- not needed yet, and would require justification in this file per brief rule 4 that the current design doesn't need); introducing Radix or a component library for the actions/tabs chrome now (rejected -- `actions`/`tabs` are plain `ReactNode` slots by design; Radix primitives are D-006's concern, when something with real focus/interaction semantics, like a Dialog, is actually built).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| DEC-048 | **Central breadcrumb manifest: prefix-walking a flat pattern registry, throws on any unregistered route, dynamic segments deliberately unregistered**                                                                                  | D-002. `lib/breadcrumbs/manifest.ts`, satisfying footgun #12 exactly: "A central route manifest mapping segments to labels, including async resolvers for entity names... Every page renders breadcrumbs through it. No page builds its own." Design: a flat array of `{namespace, pattern}` (static) or `{pattern, resolve}` (dynamic, `:param`-templated) entries; `resolveBreadcrumbs(pathname, locale)` walks every prefix of the given pathname (e.g. `/forgot-password/change` -> `[/forgot-password, /forgot-password/change]`), looks each prefix up by matching it against every registered `pattern`, and resolves its label -- via next-intl's `getTranslations({locale, namespace})` for static entries, via the entry's own `resolve()` for dynamic ones. **Throws** for any unregistered prefix rather than silently omitting a crumb: every real route must register itself here, so a gap fails loudly (confirmed live: a temporary page using an unregistered namespace produced a clean 500 with the exact message `No breadcrumb manifest entry registered for namespace '...'.`, not a silently broken page). **Nested dynamic segments deliberately not registered** (`/groups/[groupId]`, `/assignments/[id]`, `/solutions/[id]`, `/exercises/[id]`, per `docs/IA.md` §3.2's own examples): no page for any of them exists yet to test a resolver against, and their entity-name resolvers depend on group/assignment/exercise response shapes no ticket has confirmed -- F-022's typed client is ready to call core-api, but nothing has fetched one of these entities for real yet. Adding a guessed `resolve()` now would risk being wrong and needing rework; the `resolve` field's type signature (`(params, locale) => Promise<string>`) is already in place for whichever future ticket builds that route to fill in. Wired into `components/placeholder-page.tsx` via a new `resolveBreadcrumbsForNamespace(namespace, locale)` entry point -- since every stub page already self-identifies by next-intl namespace (`getTranslations("Groups")` etc.), reusing that same string as the manifest's own lookup key let all 17 stub `page.tsx` files simplify (each now just `<PlaceholderPage namespace="Groups" />`, no longer needing to be `async` or import `getTranslations` itself -- `PlaceholderPage` does one resolution and reuses the breadcrumb chain's own last crumb as the `<h1>` title, so the two can never drift apart). **Verified live** (`next dev` and a rebuilt Docker `standalone` container): the two-segment `/forgot-password/change` route correctly renders a two-crumb chain ("Reset password", linked to a properly locale-prefixed `/en/forgot-password` via `PageShell`'s `@/i18n/navigation` `Link`, then "Change forgotten password" as the unlinked current page); all 17 single-segment routes render their one crumb correctly; the full 50-test E2E suite passes unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Silently rendering an empty breadcrumb (or falling back to a raw path segment) for an unregistered route (rejected -- defeats the whole point of a _central_ manifest if a missing registration doesn't fail loudly; footgun #12 exists specifically because per-page breadcrumb construction drifts silently); guessing at group/assignment/exercise API response shapes to register their dynamic resolvers now (rejected -- no ticket has confirmed those shapes yet, and a wrong guess would need rework once the real S-/T-series ticket lands; the type signature is ready, the entries aren't, and that's an honest reflection of what's actually been verified); keying the manifest by pathname only, requiring every caller (including `components/placeholder-page.tsx`) to know its own full pathname (rejected -- namespace is already the identifier every stub page carries, and reusing it let 17 files simplify instead of growing a second, redundant piece of per-page config).                                                                                                                                                                                                                                                                                 |
| DEC-049 | **`DataTable`: client-side sort/filter/paginate over a caller-supplied dataset, URL-synced view state, self-wrapped in `Suspense`**                                                                                                    | D-003. `components/data-table.tsx`, satisfying brief §9/§10's "DataTable (sort/filter/paginate/URL sync/bulk select)" exactly. Read the legacy `repos/web-app/src/components/widgets/SortableTable` first (brief: "source code is your primary reference") rather than guessing the shape: it sorts/paginates entirely client-side over an already-fetched array, and persists sort order in `localStorage`, with **no URL state at all**. Deliberately corrected the latter -- brief §9: "Sharing a URL reproduces the exact view" -- while keeping the former (client-side operation over a caller-supplied `data: T[]`): no entity-listing ticket exists yet to know whether core-api supports server-side pagination/sort/filter for every list this will eventually back, so this component stays data-source-agnostic, same reasoning as D-002's deferred dynamic breadcrumb resolvers. Sort column+direction, filter query, and current page sync to `searchParams` (`${id}-sort`, `${id}-q`, `${id}-page`, prefixed by a required `id` prop so multiple tables can coexist on one page without colliding); row **selection** deliberately stays local React state, not URL-synced -- transient bulk-action state, not a bookmarkable view, and a long ID list in the URL is exactly the unbounded-query-string problem deep-linking isn't about. `"use client"` throughout: genuinely interactive (sortable headers, pagination, a debounced filter input, selection checkboxes), not a layout/page needing rule-4 justification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-050 | **TanStack Table not adopted for `DataTable`: v9's actual API is a headless feature-registration engine, not the simple hook the brief's stack table implies**                                                                         | D-003 follow-up. The brief's own stack table (§4) names "Tables: TanStack Table, in client leaves" as the chosen library -- not investigated before D-003 shipped with a hand-rolled implementation, a real gap noticed only afterward while re-reading §4 for D-004. Installed `@tanstack/react-table@9.1.2` (current latest, confirmed via `npm view`) and read its own bundled skill docs (`node_modules/@tanstack/react-table/skills/getting-started`, `@tanstack/table-core/skills/core`) before deciding anything, per this project's own standing rule to verify against installed docs rather than memory (the same discipline AGENTS.md's Next.js-version warnings already establish). **Finding**: v9 is a substantial rewrite from the v8 API the brief's phrasing almost certainly assumed (a simple `useReactTable({data, columns, getCoreRowModel: getCoreRowModel()})` call). V9 replaces this with an explicit, headless feature-registration model -- `useTable({features, columns, data})`, where sorting, filtering, pagination, and even reactivity bindings (`storeReactivityBindings()`) are each opt-in feature plugins passed to `tableFeatures({...})` before their state or methods exist at all -- confirmed directly from the bundled `getting-started` and `core` skill docs, not assumed from v8-era training knowledge (the docs themselves flag "Copying the v8 table constructor" as a HIGH-severity common mistake). Critically, none of this reduces the actual work this ticket needed: TanStack Table has no built-in URL-sync mechanism in any version, so the `searchParams` wiring D-003 already built (DEC-049) would be exactly as much custom code on top of TanStack Table's controlled-state API as on top of the hand-rolled version -- adopting it would mean learning and correctly wiring an entirely new headless paradigm (feature registration, row models, reactivity bindings) for zero reduction in the URL-sync code that's this component's actual hard part, in exchange for capabilities (virtualization, column pinning/grouping/faceting) no current ReCodEx list view needs. **Decision**: keep D-003's hand-rolled implementation. This is the same category of decision as F-002's TypeScript 6.0.3 pin -- a brief-specified choice investigated properly and found impractical for a _verified_, documented reason, not skipped by assumption.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Adopting TanStack Table v9 as specified (rejected after reading its actual API -- the onboarding cost of its new feature-registration paradigm doesn't buy back any of the URL-sync work, which stays entirely custom either way, and none of its distinguishing capabilities are needed yet); pinning an older v8 release instead, matching the simpler API the brief likely assumed (rejected -- deliberately installing a superseded major version to chase a specific remembered API shape is the same mistake as writing `middleware.ts` from memory instead of checking the current docs, just inverted; if a future ticket's real needs genuinely call for a headless table engine's specific capabilities, that ticket should evaluate the then-current TanStack Table on its own merits rather than this decision reaching for an old version now).                                                                                                                                                                                                                                                                                                                                                                                                                       |
| DEC-051 | **Form kit: RHF `onSubmit` calling a Server Action directly (not `<form action>`), schema split out of the `"use server"` file**                                                                                                       | D-004. `lib/forms/{action-result,use-server-action-form,use-dirty-guard}.ts`, `components/form/{text-field,form-error}.tsx`. Satisfies brief §4/§9/§10's "React Hook Form + Zod (shared schema), submitting through a Server Action" and BACKLOG.md's "field, error surfacing, pending state, dirty guard". Verified `react-hook-form@7.85.0`/`@hookform/resolvers@5.9.1` first, given DEC-050's TanStack Table lesson still fresh -- both are compatible, unremarkable installs (react peer `^19`, zod peer `^4.0.0`), no repeat of that situation. `useServerActionForm()` wires `useForm` to a schema via `zodResolver` and calls the given Server Action **directly as a function** from `handleSubmit`'s callback, not via Next's native `<form action={serverAction}>`/`useActionState` pattern (`node_modules/next/dist/docs/.../forms.md`) -- RHF already owns submission (validation, dirty/touched tracking, its own `handleSubmit` event handling), so the two submission models would fight over the same event; the action dispatch is wrapped in `startTransition` per Next's own bundled guidance for a Server Action invoked outside `<form action>`. Server-side failures map onto RHF's own error model: `fieldErrors` become per-field `form.setError()` calls, `formError` becomes RHF's `root` pseudo-field (confirmed genuinely supported in `react-hook-form/dist/types/errors.d.ts`, not invented for this ticket). `TextField` covers text-shaped inputs only (text/email/password/number/search) -- other shapes added by whichever ticket needs them, not built speculatively. `useDirtyGuard` covers `beforeunload` (full page unload) only; in-app client-side navigation interception was checked against the bundled Next.js docs and found to have no supported primitive (no `useBlocker` equivalent as of 16.3) -- documented as a real, checked gap rather than built as something fragile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| DEC-052 | **Repo checkout relocated: `../recodex-web-next` sibling → `ReCOdex/repos/web-next/`, fetched by `pull-repos.sh` -- does not reopen Q-003's original violation**                                                                       | Operator-directed change, made from the compose repo side (this repo's own remote and history are untouched by it -- `git@github.com:jurja00/codeUp-web-ui.git`, still `main`, same commits). The operator wanted to move development to another machine and asked for a single-command bootstrap: clone the compose repo, run its `pull-repos.sh`, `docker compose up`, and have everything -- including this frontend -- come up without a second manual clone step. `pull-repos.sh` was extended (compose repo's own change, not this repo's) to fetch this repo the same way it fetches the upstream ReCodEx repos: into `repos/web-next`, gitignored there, independent of this repo's own name on its remote. The compose repo's `docker-compose.yaml` `web-next` service's `build.context` moved from `../recodex-web-next` to `repos/web-next` to match -- verified live, a real `pull-repos.sh` run + `docker compose build/up web-next` succeeded end to end from the new location.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| DEC-053 | **Per-partes upload: chunking stays in the browser, each chunk proxied through a Route Handler**                                                                                                                                       | D-005. `lib/upload/{limits,chunked-upload,use-file-upload}.ts`, `components/upload/file-upload.tsx`, `app/api/upload/partial/route.ts`, `app/api/upload/partial/[id]/route.ts`, `app/api/upload/[id]/digest/route.ts`. Brief §6.7 asks whether the legacy app chunks large uploads and says to reproduce it if so. **It does**, and this reproduces it: `POST /uploaded-files/partial {name,size}` → `PUT /uploaded-files/partial/{id}?offset=N` per chunk → `POST /uploaded-files/partial/{id}` to finalize → `GET /uploaded-files/{id}/digest` verified against a locally recomputed SHA-1 → `DELETE /uploaded-files/partial/{id}` to cancel, with the legacy's own adaptive chunk sizing (64 KiB start, ×2 under 2 s, ÷2 over 4 s, clamped to [4 KiB, 4 MiB]). Read out of `repos/web-app/src/redux/modules/upload.js` + `containers/UploadContainer/UploadContainer.js` and cross-checked against core-api's `UploadedFilesPresenter` (`actionStartPartial`/`actionAppendPartial`/`actionCompletePartial`/`actionCancelPartial`/`actionDigest`) rather than trusting the client alone. **The chunk loop runs in the browser, not on the server**: brief §6.7's wording would also permit "POST the whole file to a Route Handler and let it chunk", but three of this flow's four user-visible properties only exist if the browser keeps the bytes -- real progress (a server-side loop can only report browser→Next transfer, which finishes long before core-api has the file), cancel that stops work actually in flight, and above all the checksum check, which verifies nothing if both sides of the comparison derive from the same server-side copy. The Route Handlers are therefore thin authenticated proxies -- the token is read from the httpOnly cookie server-side on every chunk and never reaches client JS (DEC-021 intact), and the chunk body is forwarded as an unbuffered stream (`duplex: "half"`, required by undici for a stream body), which is still exactly the "Route Handler that streams to core-api" §6.7 mandates and the reason this isn't a Server Action (~1 MB body limit). Size and filename are validated in three places -- component, orchestrator, Route Handler -- because a Route Handler is a public HTTP endpoint and §3.4's "a hidden button is not authorisation" applies to limits too. **One deliberate divergence from the legacy app**: local digest verification degrades to a skip when `crypto.subtle` is unavailable (it only exists in a secure context, so on plain HTTP reached by hostname -- exactly how this stack runs locally -- it is `undefined`); the legacy app throws a bare `TypeError` there. Failing an upload that genuinely succeeded, over a check the environment cannot perform, is the worse behaviour: by that point the file is already complete and durable server-side.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Whole-file POST to a Route Handler that chunks server-side (rejected -- kills real progress, in-flight cancel, and makes the checksum check self-referential, for the sole benefit of one round trip instead of N); no chunking at all, one 512 MiB request (rejected -- this is precisely the "expensive surprise" §6.7 warns about, and it would diverge from an upload protocol core-api already implements and the legacy client already exercises); reproducing the legacy's `TypeError` on a missing `crypto.subtle` for exact parity (rejected -- see the divergence note; recorded here rather than silently softened).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DEC-054 | **Dialogs on the unified `radix-ui` package; destructive confirmation on `AlertDialog`, not `Dialog`**                                                                                                                                 | D-006. `components/dialog/{dialog,confirm-dialog}.tsx`, motion tokens in `app/globals.css`. Brief §9 names Radix for "anything with focus semantics" and a modal is the canonical case (focus trap, focus restoration, `Escape`, `aria-modal`, inert background). **Package choice:** the unified `radix-ui` (1.6.7, React 19 peer verified before installing, per DEC-050's lesson) rather than `@radix-ui/react-dialog`, because D-007 (toasts), D-015 (command palette) and ordinary dropdown/tooltip/tabs work all need primitives from the same family -- one version that moves together beats a dozen independently-drifting ranges, and the tree-shaken output is per-primitive either way. **`AlertDialog` for confirmations** is a behavioural choice, not a naming one: it renders `role="alertdialog"`, moves initial focus to Cancel rather than the first focusable, and refuses outside-click dismissal (`onPointerDownOutside`/`onInteractOutside` are overridden with `preventDefault()`, read out of the installed source, not assumed) -- exactly the three properties a "delete this group?" prompt needs, and all three would have to be re-derived on top of the plain `Dialog`. **Two stale-by-a-version details checked instead of copied from memory:** 1.1.23 sets `aria-describedby` to `descriptionPresent ? descriptionId : undefined`, so the `aria-describedby={undefined}` incantation in every older Radix guide is obsolete here; and this version contains no `console` calls at all, so the well-known "DialogTitle is required" runtime warning no longer fires -- a missing title would now be a silent accessibility failure, which is why `title` is a required prop rather than a convention. **One real bug, found by looking at a screenshot rather than at the code:** the open/close keyframes originally animated `transform: translate(-50%,-50%) scale(...)`, but Tailwind v4's `-translate-x-1/2` compiles to the standalone `translate` property, so both applied and the dialog flew in from half a dialog-width off-centre. Fixed by animating the standalone `scale` property, which composes with `translate` instead of replacing it. Motion is disabled under `prefers-reduced-motion` (brief §9), scoped to these two elements' `animation` rather than a blanket `transition: none`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Per-primitive `@radix-ui/react-*` packages (rejected -- version drift across the many primitives this app still needs, for no smaller install once tree-shaken); vendoring shadcn/ui's dialog wholesale per DEC-004 (rejected for the same reason D-003/D-004 hand-rolled their equivalents: this repo's components are small, token-styled, and carry project-specific decisions, and shadcn's file would need editing on arrival anyway); building the confirmation on the plain `Dialog` with a hand-added `role="alertdialog"` (rejected -- the role is the least important of the three behaviours, and the other two are real logic); a blanket `prefers-reduced-motion` rule killing all transitions (rejected -- it would also kill focus-ring and hover transitions, which carry no motion-sickness risk).                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

**Distinguishing this from Q-003's original, real violation**: that one was F-001 scaffolding this app's actual source files (`package.json`, `app/`, `docs/`) directly into the compose repo's own git-tracked tree -- i.e., vendoring this repo's _history_ into the compose repo's _history_, exactly what brief §1 forbids ("do not vendor your frontend's source inside the compose repo"). This relocation is different in the way that actually matters: `repos/` is gitignored by the compose repo (confirmed: `repos/` is the first line of its `.gitignore`), so nothing about this repo's source ever enters the compose repo's own git history or commits -- this repo remains, in every sense the brief's §1 cares about, "its own repository." What changed is purely which _local filesystem path_ a `git clone`/`pull-repos.sh` happens to check it out to on a given machine, which was never the thing §1 was protecting against.

**Practical consequence for future sessions**: every `../ReCOdex/...`-style relative path reference written into this repo's own docs/comments _before_ this decision assumed the old sibling layout and is now one directory level short (the compose repo root is `../../` from here, not `../`, when checked out via `pull-repos.sh`). Fixed the still-actionable references (`docs/DECISIONS.md`, `docs/SEED_ACCOUNTS.md`, `lib/auth/session-cookie.ts`, `scripts/seed.ts`) to say "the compose repo's ..." instead of hardcoding a specific relative depth -- robust to this or any future relocation, matching the pattern `AGENTS.md` already used. Left `docs/PROGRESS.md`/`docs/BACKLOG.md`'s own historical entries describing _what was true when they were written_ untouched, since rewriting those would misrepresent the actual history of an append-only log; this entry and the `AGENTS.md` note are what a future reader should rely on for the current layout, not old log entries. `AGENTS.md` itself updated with an explicit note about the new two-levels-up relationship, since it's the one file read at the start of every future session. | Leaving this repo as a sibling directory and only fixing `pull-repos.sh`/docker-compose.yaml for the _old_ path (rejected -- the operator explicitly asked for it to live under `repos/`, matching the other fetched repos exactly, not to merely automate cloning a sibling); moving this repo's actual Dockerfile out to the compose repo's `services/web-next/` to fully match the upstream `services/<name>/Dockerfile` + `COPY repos/<name>/...` pattern the brief originally described (considered, rejected for now -- DEC-026's original reasoning for a self-contained Dockerfile still holds independent of where the source happens to be checked out: this repo's own CI already builds and tests this exact Dockerfile standalone, which would regress if the Dockerfile moved into the compose repo and started assuming a `repos/web-next` COPY source that only exists there; revisit only if a concrete reason to unify the pattern shows up); rewriting old `../ReCOdex`-style path references in `docs/PROGRESS.md`/`docs/BACKLOG.md`'s historical entries to match the new layout (rejected -- those entries are accurate records of what was true when written; correcting them would be revising history, not fixing a stale reference). |

**One real bug found live, the same category as D-003's two**: a first version defined the Zod schema in the same file as the `"use server"` action (natural, since brief says "shared schema"). This produced a genuine runtime error -- `zodResolver` throwing "Invalid input: not a Zod schema" -- because a `"use server"` file's compiler pass only handles (async) function exports; the co-located schema object is silently replaced with something else by the time the client imports it. Fixed by moving the schema into its own plain module, imported by both the client form and the action file -- documented prominently in `use-server-action-form.ts`'s own doc comment so the next form-building ticket doesn't rediscover this the same way.

**Verified live** (both `next dev` and, specifically given D-002/D-003's pattern of Docker-only failures, a rebuilt Docker `standalone` container) via a real demo Server Action exercising all four paths in one flow: client-side Zod validation (empty submit shows a field error, no server round trip); a server-only field error ("taken" -> per-field message a client schema alone couldn't produce); a server-only form-level error ("forbidden" -> the `root` error, rendered by `FormError`); a success path; and the dirty-guard's tracked state genuinely flipping from clean to dirty on input. | Next's native `<form action={serverAction}>` + `useActionState` for these forms too (rejected -- that's the right pattern for _simple_ forms per the brief's own stack table row ("Complex forms: React Hook Form + Zod"), but two systems both trying to own form submission and error state would conflict; RHF's `handleSubmit` calling the action as a plain function sidesteps this entirely); building every field type (select, checkbox, textarea, radio) now (rejected -- no concrete consuming form has asked for them yet; `TextField` covers what's needed today, more added the same way when a real ticket needs one); a custom Link-click interceptor to approximate in-app dirty-guard navigation blocking (rejected -- checked the bundled docs first and found no supported primitive to build on; a hand-rolled interceptor would need to independently rediscover every way a navigation can start and would drift out of sync with the router, exactly the kind of fragile workaround the brief's own verification discipline argues against). |

**Two real bugs found live, both by actually building and testing this rather than assuming the design was right on paper:**

(1) A first version passed `columns` (with `cell`/`sortValue`/`filterValue` function props) from a Server Component debug page straight into `<DataTable>` -- a real "Functions cannot be passed directly to Client Components" runtime error, since React can't serialize a function across the RSC boundary. Documented prominently in the component's own doc comment: `columns` must be defined inside a `"use client"` wrapper the caller owns (the Server Component fetches `data`, a small client component defines `columns` and renders `<DataTable>`), not passed down from the page itself. This is the shape every future S-/T-/AD- list-view ticket will need to follow.

(2) A local `next dev` session and even a local ad-hoc `next build` (run without the debug route present at the time) both looked clean, but rebuilding the Docker image with a debug route present failed at prerender: "useSearchParams() should be wrapped in a suspense boundary." `next dev` has no static-generation step to enforce this against, so it silently never caught it -- only a real production build did, and specifically the Docker build this project already treats as the authoritative production check (same category of dev/production divergence as DEC-039's `request.url` bug). Fixed by having `DataTable` wrap its own `useSearchParams()`-reading implementation in an internal `<Suspense fallback={null}>`, so every future caller just imports `DataTable` and it works, rather than every consuming ticket needing to remember its own boundary.

**Verified live**, specifically re-verified against a rebuilt Docker `standalone` container after the `Suspense` fix (not just `next dev`, given finding #2 above): sort cycles correctly through ascending -> descending -> cleared across three header clicks, each state reflected in the URL; pagination advances and is reflected in the URL; a debounced filter narrows the dataset, composes correctly with pagination (11 matches, pageSize 10 -> exactly 10 rows shown), and resets to page 1; clearing the filter restores the full set; selecting a row toggles its checkbox and leaves the URL completely unchanged; the header "select all" checkbox correctly selects every row on the current page. | Server-side pagination/sort/filter query params sent to core-api directly (rejected -- no ticket has confirmed whether core-api's list endpoints even support this per-entity yet; premature for a Design System ticket that predates every actual list-view ticket); persisting sort order in `localStorage` like the legacy `SortableTable` (rejected -- exactly the pattern brief §9 calls out wanting corrected: "Deep-linkable everything... Sharing a URL reproduces the exact view", which `localStorage` state can't do); URL-syncing row selection alongside sort/filter/page (rejected -- selection is transient bulk-action UI state, not a view worth bookmarking, and could grow unboundedly long); requiring every caller to wrap `<DataTable>` in its own `<Suspense>` (rejected once the internal-wrapping alternative was implemented and verified -- strictly better ergonomics for every future consumer, no downside). |

---

## Deferred

| #       | Decision                                       | Reason for Deferral                                                                                                    | Revisit When                                     |
| ------- | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| DEF-001 | **`cacheComponents` and `partialPrefetching`** | Brief §4: essentially every byte is per-user and permission-dependent. Implicit caching is wrong bet until app exists. | After parity is reached, static shell identified |
| DEF-002 | **React Compiler**                             | Brief §4: measure first. Enable only after profiling shows benefit.                                                    | After performance baseline established           |
| DEF-003 | **Graphviz rendering approach**                | Brief §7: decide early but budget significant time. Port viz.js initially, evaluate alternatives.                      | After pipeline editor is functional              |
| DEF-004 | **WebSocket for evaluation progress**          | Brief §7: decide after checking compose repo. Polling is simpler, WebSocket is real-time.                              | After monitor service configuration confirmed    |
| DEF-005 | **Extension token handoff**                    | Brief §7: investigate, don't silently drop or hand out token.                                                          | After SIS-ext-webapp mechanism understood        |

---

## Dropped

| #        | Item                             | Reason                                                                                       |
| -------- | -------------------------------- | -------------------------------------------------------------------------------------------- |
| DROP-001 | **SKIN config**                  | AdminLTE color skin. Replaced by proper Tailwind dark/light theme. Recorded in `DROPPED.md`. |
| DROP-002 | **Redux, redux-form, immutable** | Legacy state management. Replaced by Server Components, Server Actions, React Hook Form.     |
| DROP-003 | **moment.js**                    | Deprecated. Replaced by `date-fns` + native `Intl`.                                          |
| DROP-004 | **moment-timezone**              | Replaced by server-side absolute dates + client-side relative formatting.                    |
| DROP-005 | **Webpack/Babel**                | Replaced by Next.js built-in Turbopack. No custom build config needed.                       |

---

## Assumptions (Tagged `IA-ASSUMPTION`)

| #       | Assumption                                | Where Used                        | If Wrong                                   |
| ------- | ----------------------------------------- | --------------------------------- | ------------------------------------------ |
| ASS-002 | New app binds to port `3000`              | Dockerfile, compose               | Update `PORT` env and compose port mapping |
| ASS-004 | Single instance deployment                | Auth BFF, `instanceId` handling   | Add instance selector to login flow        |
| ASS-008 | SMTP not configured, use `mail.debugMode` | Registration/password reset flows | Configure real SMTP, test end-to-end       |

**ASS-007 was right and is now verified** (D-005), so it has left this table: the 512 MiB ceiling is read directly from the compose repo's `services/proxy/nginx.conf.template` and `services/api/nginx-site.conf` (`client_max_body_size 512M`) and `services/api/php-recodex.ini` (`upload_max_filesize`/`post_max_size = 512M`), and lives in one place in code (`MAX_UPLOAD_BYTES`, `lib/upload/limits.ts`). Worth noting core-api's own `actionStartPartial()` allows a _larger_ file than that (1 GiB) -- this deployment's nginx/PHP layer is the binding constraint, not the API, so raising the ceiling means changing the compose repo, not this one.

**ASS-001, ASS-003, ASS-005, ASS-006 were wrong** and are removed from this table rather than
corrected in place — see `QUESTIONS.md`'s Resolved section (Q-001, Q-004, Q-006, Q-010) for the
actual verified values. Removing rather than editing keeps this table honest about what was
_assumed and risky_ versus _known_; the "If Wrong" column already did its job for these four.

**Process note:** all four were guesses about _this specific running deployment_ (its public URL,
its CAS config, its monitor wiring, its API-doc exposure) that `ReCOdex/.env` and
`ReCOdex/docker-compose.yaml` answer directly — no guessing needed. Recon read the legacy
_application source_ thoroughly but not the _deployment_ this app is meant to run alongside. Both
count as "the compose repo" per §1's inputs list, and both need reading before an environment
assumption goes in this table.
